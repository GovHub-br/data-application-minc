version: 2

models:
  - name: sac__aberturadecontabancaria
    description: >
      Tabela responsável por armazenar informações sobre a abertura de contas bancárias
      vinculadas aos projetos, incluindo status da conta, datas de liberação/bloqueio, órgão
      responsável e identificação do projeto. Camada bronze tipada a partir de
      salic_bronze.sac__aberturadecontabancaria.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: contabloqueada
        description: >
          0.Não;1.Sim
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcontabloqueada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: contalivre
        description: >
          0.Não;1.Sim
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcontalivre
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtnascimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idaberturadeconta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aberturadecontasav39215
    description: >
      Tabela responsável por registrar informações auxiliares do tipo SAV39215 utilizadas
      como complemento no processo de abertura de contas bancárias. Camada bronze tipada a
      partir de salic_bronze.sac__aberturadecontasav39215.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aberturadecontasav39224
    description: >
      Tabela responsável por registrar informações auxiliares do tipo SAV39224 relacionadas
      ao fluxo de abertura de contas. Camada bronze tipada a partir de
      salic_bronze.sac__aberturadecontasav39224.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aberturadecontasav39225
    description: >
      Tabela utilizada para armazenar dados complementares referentes ao tipo SAV39225 no
      processo de abertura de contas. Camada bronze tipada a partir de
      salic_bronze.sac__aberturadecontasav39225.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aberturadecontasav39227
    description: >
      Tabela responsável por armazenar registros adicionais do tipo SAV39227 usados como
      apoio ao processo bancário. Camada bronze tipada a partir de
      salic_bronze.sac__aberturadecontasav39227.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aberturadecontaseficpf40979
    description: >
      Tabela responsável por registrar informações do modelo SEFIC Pessoa Física 40979,
      utilizadas em procedimentos específicos de abertura de conta. Camada bronze tipada a
      partir de salic_bronze.sac__aberturadecontaseficpf40979.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aberturadecontaseficpf40980
    description: >
      Tabela utilizada para armazenar dados auxiliares do tipo SEFIC Pessoa Física 40980
      relacionados à abertura de contas. Camada bronze tipada a partir de
      salic_bronze.sac__aberturadecontaseficpf40980.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aberturadecontaseficpj40975
    description: >
      Tabela responsável por armazenar informações do modelo SEFIC Pessoa Jurídica 40975
      utilizadas em processos bancários de abertura de contas. Camada bronze tipada a partir
      de salic_bronze.sac__aberturadecontaseficpj40975.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aberturadecontaseficpj40981
    description: >
      Tabela utilizada para registrar dados complementares do tipo SEFIC Pessoa Jurídica
      40981 no fluxo de abertura de contas. Camada bronze tipada a partir de
      salic_bronze.sac__aberturadecontaseficpj40981.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__abrangencia
    description: >
      Tabela responsável por registrar a abrangência territorial dos projetos, incluindo
      país, UF, município, datas de realização, justificativas e status. Camada bronze tipada
      a partir de salic_bronze.sac__abrangencia.
    columns:
      - name: idabrangencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpais
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipioibge
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stabrangencia
        description: >
          Representa o status da abrangência sendo 0 para desativado e 1 para ativado.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: siabrangencia
        description: >
          Diz se realizou ou não o evento na localidade indicada. 0 - Sem informação 1 - Não,
          2 - Sim.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          Descreve o por que do evento não ter sido realizado no local descrito.
        data_type: text
      - name: dtiniciorealizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimrealizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__acaoproduto
    description: >
      Tabela responsável por armazenar informações sobre produtos vinculados às ações dos
      projetos, como quantidades produzidas, recebidas, estoque e localização. Camada bronze
      tipada a partir de salic_bronze.sac__acaoproduto.
    columns:
      - name: entidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: codigoproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdeproduzida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtderecebida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdeexistente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: localizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__acaoprojeto
    description: >
      Tabela responsável por registrar as ações vinculadas aos projetos, contendo informações
      como tipo, nome, localização, área, segmento, datas, mecanismo e entidade responsável.
      Camada bronze tipada a partir de salic_bronze.sac__acaoprojeto.
    columns:
      - name: entidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcriacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: sumario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descentidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__acompanhamentobandasx
    description: >
      Tabela responsável por armazenar dados detalhados de acompanhamento dos projetos do
      tipo BandasX, incluindo avaliações, métricas institucionais e informações específicas
      de execução. Camada bronze tipada a partir de salic_bronze.sac__acompanhamentobandasx.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitentregue')
                field: anoprojeto
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitentregue')
                field: sequencial
      - name: instituicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo07
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo08
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo09b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo09c
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo10a
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo10b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo10c
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo10d
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo11a
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo11b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo11c
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo11d
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo11e
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo13
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo14
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo15
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo16
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo17
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo18
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo19a
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo19b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo19c
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo19d
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo20a
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo20b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo20c
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo20d
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo20e
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo20f
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo20g
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo20h
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo21
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo22
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo23
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo24
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo25a
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo25b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo26
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo27a
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo27b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo27c
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo27d
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo27e
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo27f
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo27g
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo27h
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo29a
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo29b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo29c
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo29d
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo29e
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo30a
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo30b
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo30c
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo30d
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: responsavelbanda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: maestrobanda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: formacaomaestro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: auditadasfci
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__agenciasbb
    description: >
      Tabela responsável por armazenar informações das agências do Banco do Brasil, incluindo
      nome, UF, perfil, endereço completo e município. Camada bronze tipada a partir de
      salic_bronze.sac__agenciasbb.
    columns:
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: logradouro_completo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aprovacao
    description: >
      Tabela responsável por registrar os dados de aprovação dos projetos, incluindo
      portarias, valores aprovados, datas e tipo de aprovação. Camada bronze tipada a partir
      de salic_bronze.sac__aprovacao.
    columns:
      - name: idaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__aprovacao')
                field: anoprojeto
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__aprovacao')
                field: sequencial
      - name: tipoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__aprovacao')
                field: tipoaprovacao
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__aprovacao')
                field: dtaprovacao
      - name: resumoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: portariaaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtportariaaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtpublicacaoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: aprovadoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovadoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: autorizadoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: autorizadoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: concedidocusteioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: concedidocapitalreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartidareal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprorrogacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__area
    description: >
      Tabela utilizada para armazenar as áreas classificatórias dos projetos, com código,
      descrição e status. Camada bronze tipada a partir de salic_bronze.sac__area.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stestado
        description: >
          0.Desativado; 1.ativo
        data_type: boolean
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__aspectosfisicosx
    description: >
      Tabela responsável por armazenar dados referentes aos aspectos físicos dos projetos,
      com campos avaliativos e justificativas. Camada bronze tipada a partir de
      salic_bronze.sac__aspectosfisicosx.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campo_01
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: campo_02
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: campo_03
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: campo_04
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: justificativas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__atualizaorgaoprojetosx
    description: >
      Tabela utilizada para armazenar atualizações do órgão responsável pelos projetos,
      vinculada ao PRONAC. Camada bronze tipada a partir de
      salic_bronze.sac__atualizaorgaoprojetosx.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__auditoria
    description: >
      Tabela responsável por registrar informações de auditoria interna, contendo nome do
      banco, schema, tipo de objeto e instruções executadas. Camada bronze tipada a partir de
      salic_bronze.sac__auditoria.
    columns:
      - name: database_name
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: schema_name
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: class_type
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: object_name
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: statement
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__autuacaox
    description: >
      Tabela responsável por armazenar registros de autuação do projeto, como datas, folhas,
      providências e observações. Camada bronze tipada a partir de
      salic_bronze.sac__autuacaox.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtautuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: folhas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: providencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: obs
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__avaliacaopc
    description: >
      Tabela responsável por armazenar avaliações de prestação de contas, incluindo analista,
      data, campos técnicos, resultado e pronunciamentos. Camada bronze tipada a partir de
      salic_bronze.sac__avaliacaopc.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: prestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: analista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_0
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_01
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_02
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_03
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_04
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_05
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_06
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_07
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_08
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_09
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_010
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_011
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_012
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_013
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_014
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_015
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_016
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_017
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_018
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_019
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_020
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_021
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_022
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_023
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_024
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_025
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo_026
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: resultado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: manifestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronunciamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: providencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__avaliacaoprojeto
    description: >
      Tabela utilizada para armazenar avaliações gerais dos projetos, com parâmetros e pesos
      utilizados. Camada bronze tipada a partir de salic_bronze.sac__avaliacaoprojeto.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: codparametro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: codpeso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__avaliacaoproponente
    description: >
      Tabela responsável por registrar avaliações vinculadas ao proponente, com parâmetros e
      pesos aplicados. Camada bronze tipada a partir de
      salic_bronze.sac__avaliacaoproponente.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: codparametro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: codpeso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__bancoagencia
    description: >
      Tabela responsável por armazenar dados de agências bancárias, incluindo endereço, UF,
      telefone, fax, perfil e status. Camada bronze tipada a partir de
      salic_bronze.sac__bancoagencia.
    columns:
      - name: banco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: fax
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__bancoagencia20100824
    description: >
      Tabela utilizada para armazenar o snapshot das agências bancárias referente à
      atualização de 24/08/2010. Camada bronze tipada a partir de
      salic_bronze.sac__bancoagencia20100824.
    columns:
      - name: banco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: fax
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__bancoagencia_08
    description: >
      Tabela auxiliar responsável por armazenar dados antigos de agências bancárias (versão
      08). Camada bronze tipada a partir de salic_bronze.sac__bancoagencia_08.
    columns:
      - name: banco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__bancoagencia_bkp_04112009x
    description: >
      Tabela utilizada como backup histórico das agências bancárias cadastradas até
      04/11/2009. Camada bronze tipada a partir de
      salic_bronze.sac__bancoagencia_bkp_04112009x.
    columns:
      - name: banco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: fax
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__bancoagencianovo1
    description: >
      Tabela complementar usada para armazenar variações da nova estrutura de cadastro de
      agências bancárias. Camada bronze tipada a partir de
      salic_bronze.sac__bancoagencianovo1.
    columns:
      - name: banco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__bandas
    description: >
      Tabela responsável por armazenar informações sobre bandas vinculadas a projetos,
      incluindo finalidade, data de fundação, número de componentes e pontuações
      regionais/nacionais. Camada bronze tipada a partir de salic_bronze.sac__bandas.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: finalidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfundacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: numerocomponentes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: regente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: indicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: emenda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: pmunicipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: pestadual
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: pregional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: pnacional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: pinternacional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__bcoagmar2011_
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__bcoagmar2011$.
    columns:
      - name: bco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: fone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: fax
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__bolsa
    description: >
      Tabela responsável por armazenar dados de bolsas concedidas, incluindo país,
      instituição, orientador, datas de início e fim, e contatos. Camada bronze tipada a
      partir de salic_bronze.sac__bolsa.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pais
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: instituicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: instcidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: instendereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: instcep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: insttelefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: instemail
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: orientador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioajuda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimajuda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: portfolio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__captacao
    description: >
      Tabela responsável por registrar as captações de recursos dos projetos, incluindo
      recibos, datas, valores captados, tipo de apoio e informações do mecenas. Camada bronze
      tipada a partir de salic_bronze.sac__captacao.
    columns:
      - name: idcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: numerorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpfmecena
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__interessado')
                field: cgccpf
      - name: tipoapoio
        description: >
          patrocinio ou doação 1 Doação 2 Patrocínio
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: medidaprovisoria
        description: >
          1 - artigo 26 (lei 8.313/91) e 2 - artigo 18 MedidaProvisoria : 1.Artigo 26 e
          2.100%
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtchegadarecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sitransferenciarecurso
        description: >
          0. Recurso Bloqueado; 1.Recurso Liberado
        data_type: text
      - name: dttransferenciarecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: isbemservico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__captacaoconversao
    description: >
      Tabela responsável por registrar conversões de valores captados, incluindo data, número
      do recibo e valor convertido. Camada bronze tipada a partir de
      salic_bronze.sac__captacaoconversao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: numerorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtconversao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__captacaoguia
    description: >
      Tabela responsável por armazenar informações de guias de captação, com datas, números,
      valores e observações. Camada bronze tipada a partir de salic_bronze.sac__captacaoguia.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: numeroguia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__guiarecolhimento')
                field: numeroguia
      - name: dtcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__captacaoold
    description: >
      Tabela utilizada como histórico das captações, preservando registros antigos com mesma
      estrutura da tabela atual. Camada bronze tipada a partir de
      salic_bronze.sac__captacaoold.
    columns:
      - name: idcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: numerorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpfmecena
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__interessado')
                field: cgccpf
      - name: tipoapoio
        description: >
          patrocinio ou doação 1 Doação 2 Patrocínio
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: medidaprovisoria
        description: >
          1 - artigo 26 (lei 8.313/91) e 2 - artigo 18 MedidaProvisoria : 1.Artigo 26 e
          2.100%
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtchegadarecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sitransferenciarecurso
        description: >
          0. Recurso Bloqueado; 1.Recurso Liberado
        data_type: text
      - name: dttransferenciarecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: isbemservico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__captacaoquotas
    description: >
      Tabela responsável por registrar captação via quotas, incluindo integração, datas,
      quantidade e dados do subscritor. Camada bronze tipada a partir de
      salic_bronze.sac__captacaoquotas.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anocav
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencialcav
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nintegra
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpfsub
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtintegraliza
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdquotasintegr
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__carregarecibocaptacaodanca
    description: >
      Tabela responsável por carregar registros de recibos de captação para projetos de
      Dança, contendo dados do mecenas, valores e datas. Camada bronze tipada a partir de
      salic_bronze.sac__carregarecibocaptacaodanca.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: operacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: numerorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: datarecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: cpfcnpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: ddd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: pfpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__carregarecibocaptacaodanca2010
    description: >
      Tabela utilizada para armazenar recibos de captação de dança referentes ao ano-base
      2010. Camada bronze tipada a partir de
      salic_bronze.sac__carregarecibocaptacaodanca2010.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: operacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: numerorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: datarecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cpfcnpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: ddd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: pfpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__carregarecibocaptacaodanca2012
    description: >
      Tabela utilizada para armazenar recibos de captação de dança do ano-base 2012. Camada
      bronze tipada a partir de salic_bronze.sac__carregarecibocaptacaodanca2012.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: operacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: numerorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: datarecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: cpfcnpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: ddd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: pfpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__carregarecibocaptacaodanca2013
    description: >
      Tabela responsável por armazenar recibos de captação de dança referentes ao ano-base
      2013. Camada bronze tipada a partir de
      salic_bronze.sac__carregarecibocaptacaodanca2013.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: operacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: numerorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: datarecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cpfcnpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: ddd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: pfpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__carta
    description: >
      Tabela responsável por armazenar tipos de cartas institucionais, com código, descrição
      e status. Camada bronze tipada a partir de salic_bronze.sac__carta.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__cartaeventual
    description: >
      Tabela responsável por armazenar cartas eventuais emitidas para projetos, com data,
      texto, órgão e logon. Camada bronze tipada a partir de salic_bronze.sac__cartaeventual.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcartaeventual
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__certidoesnegativas
    description: >
      Tabela responsável por registrar certidões negativas relacionadas aos projetos,
      incluindo emissão, validade, protocolo e situação. Camada bronze tipada a partir de
      salic_bronze.sac__certidoesnegativas.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__interessado')
                field: cgccpf
      - name: codigocertidao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtemissao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtvalidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idcertidoesnegativas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: cdprotocolonegativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdsituacaocertidao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__cnic
    description: >
      Tabela responsável por armazenar informações das reuniões da CNIC, incluindo número,
      data, pauta, resultados e observações. Camada bronze tipada a partir de
      salic_bronze.sac__cnic.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipodepauta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: resultadodaanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__complementacao
    description: >
      Tabela responsável por registrar pedidos de complementação de recursos dos projetos,
      incluindo valores solicitados e justificativas. Camada bronze tipada a partir de
      salic_bronze.sac__complementacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pedido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcomplementacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitadoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitadoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitadocusteioufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitadocusteioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitadocapitalufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitadocapitalreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: atendimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__conselheiro
    description: >
      Tabela responsável por armazenar os conselheiros cadastrados, com nome, condição e
      status. Camada bronze tipada a partir de salic_bronze.sac__conselheiro.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: condicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: status
        description: >
          0.Ativo;1.Inativo
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__consultor
    description: >
      Tabela responsável por armazenar consultores, com nome, área de atuação e status.
      Camada bronze tipada a partir de salic_bronze.sac__consultor.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__consultorespc
    description: >
      Tabela responsável por armazenar consultores vinculados à Prestação de Contas,
      incluindo datas, volume, observações e vínculos com PRONAC. Camada bronze tipada a
      partir de salic_bronze.sac__consultorespc.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsaida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtretorno
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: consultor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtdevolume
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__contabancaria
    description: >
      Tabela responsável por armazenar informações de contas bancárias vinculadas aos
      projetos, incluindo conta bloqueada/livre, lotes, ocorrências e banco/agência. Camada
      bronze tipada a partir de salic_bronze.sac__contabancaria.
    columns:
      - name: idcontabancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: banco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: contabloqueada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtloteremessacb
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: loteremessacb
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ocorrenciacb
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: contalivre
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtloteremessacl
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: loteremessacl
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ocorrenciacl
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__convenio
    description: >
      Tabela responsável por registrar convênios associados aos projetos, com datas,
      vigência, objeto, valor e dados de publicação. Camada bronze tipada a partir de
      salic_bronze.sac__convenio.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: numeroconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtpublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfinalexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtiniciovigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfinalvigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: objeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valorconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__criteriosx
    description: >
      Tabela responsável por armazenar critérios utilizados na análise de projetos do tipo X,
      incluindo prioridade, tipo de apoio e justificativas. Camada bronze tipada a partir de
      salic_bronze.sac__criteriosx.
    columns:
      - name: idcriterios
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtcriterio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: prioridade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tipoapoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idparecervinculdas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__curriculo
    description: >
      Tabela responsável por armazenar currículos de proponentes ou participantes, incluindo
      nível, CPF/CNPJ e logon. Camada bronze tipada a partir de salic_bronze.sac__curriculo.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__interessado')
                field: cgccpf
      - name: nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cadastrosav
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_captacaoanoufmunicipio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_captacaoanoufmunicipio.
    columns:
      - name: ano_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral.
    columns:
      - name: projeto_pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_projeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_resumo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_celular
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_comercial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_email
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_responsavel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_data_inicio_execucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_data_fim_execucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_data_inicio_execucao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_data_fim_execucao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_valor_solicitado_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: aprovacao_data_aprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_data_aprovacao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_data_inicio_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_data_fim_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_data_inicio_captacao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_data_fim_captacao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_valor_aprovado_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_numero_recibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_tipo_apoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_data_recibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: captacao_data_recibo_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_valor_captacao_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: investidor_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: investidor_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: municipio_abrangencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: uf_municipio_abrangencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: convenio_valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_2008
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_2008.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_2009
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_2009.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_2010
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_2010.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_2011
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_2011.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_2012
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_2012.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_2013
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_2013.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_2014
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_2014.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_fred
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_fred.
    columns:
      - name: projeto_pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_projeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nu_pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_resumo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_celular
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_comercial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_email
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_responsavel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_data_inicio_execucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: projeto_data_fim_execucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: projeto_data_inicio_execucao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_data_fim_execucao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_valor_solicitado_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: aprovacao_data_aprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: aprovacao_data_aprovacao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_data_inicio_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: aprovacao_data_fim_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: aprovacao_data_inicio_captacao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_data_fim_captacao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_valor_aprovado_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: captacao_numero_recibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_tipo_apoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_data_recibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: captacao_data_recibo_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_valor_captacao_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: investidor_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: investidor_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: municipio_abrangencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: uf_municipio_abrangencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: convenio_valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_geral_sem_abrangencia
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_geral_sem_abrangencia.
    columns:
      - name: projeto_pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_projeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_data_inicio_execucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: projeto_data_fim_execucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: projeto_data_inicio_execucao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_data_fim_execucao_formatado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: projeto_valor_solicitado_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: aprovacao_data_inicio_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_data_fim_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: aprovacao_valor_aprovado_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: captacao_numero_recibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_tipo_apoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_data_recibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacao_valor_captacao_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: investidor_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_qtd_apresentado_aprovado_ano_mes
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_qtd_apresentado_aprovado_ano_mes.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidade_apresentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidade_aprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidade_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_qtd_apresentado_aprovado_captado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_qtd_apresentado_aprovado_captado.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: setor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtd_apresentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtd_aprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtd_captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_qtd_proponentes_inabilitados
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_qtd_proponentes_inabilitados.
    columns:
      - name: qtd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_relacao_projetos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_relacao_projetos.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: projeto_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: proponente_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: proponente_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: ano_apresentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mes_apresentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_apoiado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: situacao_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano_situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: mes_situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cidade_execucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_relacao_projetos_fred
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_relacao_projetos_fred.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano_projeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipo_apoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: secretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: medida_prov
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: ano_apresentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: mes_apresentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: data_aprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valor_solicitado_real
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: valor_aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_apoiado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: valor_captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: captacao_numero_recibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: investidor_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: investidor_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: situacao_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano_situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: mes_situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cidade_execucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_relacao_projetos_geral
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_relacao_projetos_geral.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: projeto_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: proponente_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: proponente_cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: ano_apresentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mes_apresentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: valor_apoiado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: situacao_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano_situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: mes_situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_tcu_captacao_sav
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_tcu_captacao_sav.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_tcu_captacao_sefic
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_tcu_captacao_sefic.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_total_investido_investidor_ano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_total_investido_investidor_ano.
    columns:
      - name: ano_investimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_investido_total
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_valor_solicitado_captado_projeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_valor_solicitado_captado_projeto.
    columns:
      - name: projeto_enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto_situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_solicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: ano_recibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dba_valor_solicitado_captado_projeto_sav
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__dba_valor_solicitado_captado_projeto_sav.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano_protocolo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dbf
    description: >
      Tabela responsável por armazenar registros genéricos em formato DBF, contendo tipo de
      registro, ano, sequencial e informações básicas. Camada bronze tipada a partir de
      salic_bronze.sac__dbf.
    columns:
      - name: tiporegistro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dbf_agosto
    description: >
      Tabela utilizada para armazenar informações provenientes de arquivos DBF específicos do
      período de agosto. Camada bronze tipada a partir de salic_bronze.sac__dbf_agosto.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dbf_dezembro
    description: >
      Tabela utilizada para armazenar informações provenientes de arquivos DBF específicos do
      período de dezembro. Camada bronze tipada a partir de salic_bronze.sac__dbf_dezembro.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dbf_maio
    description: >
      Tabela utilizada para armazenar informações provenientes de arquivos DBF específicos do
      período de maio. Camada bronze tipada a partir de salic_bronze.sac__dbf_maio.
    columns:
      - name: informacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__deorgaoparaorgao
    description: >
      Tabela responsável por registrar transferências ou encaminhamentos entre órgãos,
      indicando órgão de origem e destino. Camada bronze tipada a partir de
      salic_bronze.sac__deorgaoparaorgao.
    columns:
      - name: deorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: paraorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__distribuicao_avaliacao_proposta
    description: >
      Tabela responsável por registrar a distribuição de propostas para avaliação, incluindo
      avaliador, órgão, perfil e data. Camada bronze tipada a partir de
      salic_bronze.sac__distribuicao_avaliacao_proposta.
    columns:
      - name: id_distribuicao_avaliacao_proposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: id_preprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: id_orgao_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: id_perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: data_distribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: avaliacao_atual
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: id_avaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__distribuicao_avaliacao_proposta_bkp
    description: >
      Tabela utilizada como backup dos registros de distribuição de propostas para avaliação.
      Camada bronze tipada a partir de salic_bronze.sac__distribuicao_avaliacao_proposta_bkp.
    columns:
      - name: id_distribuicao_avaliacao_proposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__distribuicao_avaliacao_proposta')
                field: id_distribuicao_avaliacao_proposta
      - name: id_preprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: id_orgao_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: id_perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: data_distribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: avaliacao_atual
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__distribuicaopassagem
    description: >
      Tabela responsável por registrar a distribuição de passagens para projetos, incluindo
      fatura, valores concedidos, entrega e devolução de bilhetes. Camada bronze tipada a
      partir de salic_bronze.sac__distribuicaopassagem.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__interessado')
                field: cgccpf
      - name: rt
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlconcedido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: administrativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtentregabilhete
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdevolucaobilhete
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtrelatorio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__documentopessoal
    description: >
      Tabela responsável por armazenar documentos pessoais vinculados aos projetos, com
      campos variáveis e status. Camada bronze tipada a partir de
      salic_bronze.sac__documentopessoal.
    columns:
      - name: iddocumentopessoal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: seq
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: campo2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campo3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campo4
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campo5
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__documentosexigidos
    description: >
      Tabela responsável por listar documentos obrigatórios exigidos, contendo área,
      descrição, opções, estado e upload. Camada bronze tipada a partir de
      salic_bronze.sac__documentosexigidos.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Inativo;1.Ativo
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: stupload
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__documentosprojeto
    description: >
      Tabela responsável por armazenar documentos apresentados pelos projetos, com código,
      PRONAC e identificadores. Camada bronze tipada a partir de
      salic_bronze.sac__documentosprojeto.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: codigodocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__documentosexigidos')
                field: codigo
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__documentosproponente
    description: >
      Tabela responsável por registrar documentos enviados pelos proponentes, vinculados a
      CPF/CNPJ, PRONAC e projeto. Camada bronze tipada a partir de
      salic_bronze.sac__documentosproponente.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - relationships:
              arguments:
                to: ref('sac__interessado')
                field: cgccpf
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: codigodocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__documentosexigidos')
                field: codigo
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__dtproperties
    description: >
      Tabela técnica utilizada pelo SQL Server para armazenar propriedades internas do banco.
      Camada bronze tipada a partir de salic_bronze.sac__dtproperties.
    columns:
      - name: id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: objectid
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: property
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: value
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: uvalue
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: lvalue
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: version
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__edital
    description: >
      Tabela responsável por armazenar informações sobre editais, incluindo número, objeto,
      órgão, datas e parâmetros de avaliação. Camada bronze tipada a partir de
      salic_bronze.sac__edital.
    columns:
      - name: idedital
        description: >
          Sequêncial identificador do edital.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idorgao
        description: >
          Código identificador do orgão/ entidade cadastrado.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nredital
        description: >
          Número do edital a tramitar no sistema.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtedital
        description: >
          Data de criação do edital. **
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: celulaorcamentaria
        description: >
          Número identificaor da célula orçamentaria. Este atributo é proveniente do sistema
          SIMC e é composto por uma série de campos.
        data_type: text
      - name: objeto
        description: >
          Descrição ...*****
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          Identificação da pessoa que inseriu no sistema.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtavaliador
        description: >
          Define a quantidade de de avalidores por Edital
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stdistribuicao
        description: >
          Indica se a distribuição do edital para cada avaliador será manual (M) ou
          Altomática (A).
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stadmissibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdtipofundo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idati
        description: >
          Essa coluna servirá como uma espécie de FK para o campo “atiid” da tabela
          “BDSIMEC.pde.atividade”, mas não é campo obrigatório. obs.: Quando a integração
          “SIMC x SALIC” for publicada em produção é necessário publicar também a coluna
          "idAti" da tabela "SAC.dbo.Edital", isso é fundamental para a integração funcionar.
        data_type: integer
      - name: idlinguagem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idmodalidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__editaldesembolso
    description: >
      Tabela responsável por registrar desembolsos associados aos editais, incluindo
      parcelas, valores e empenhos. Camada bronze tipada a partir de
      salic_bronze.sac__editaldesembolso.
    columns:
      - name: iddesembolso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idedital
        description: >
          Sequêncial identificador do edital.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__editalparcelas')
                field: idedital
      - name: nrparcela
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__editalparcelas')
                field: nrparcela
      - name: data
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcapital
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcusteio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: pagou
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrempenhocusteio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrordembancariacusteio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__editalparcelas
    description: >
      Tabela responsável por armazenar a distribuição das parcelas previstas no edital,
      incluindo valores, faixas e exigência de PC. Camada bronze tipada a partir de
      salic_bronze.sac__editalparcelas.
    columns:
      - name: idedital
        description: >
          Sequêncial identificador do edital.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__edital')
                field: idedital
      - name: nrparcela
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: faixa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: diasparaliberar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: exigepc
        description: >
          0.Não ;1.Sim
        data_type: boolean
      - name: parcela
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__editalprojeto
    description: >
      Tabela responsável por vincular projetos a editais específicos. Camada bronze tipada a
      partir de salic_bronze.sac__editalprojeto.
    columns:
      - name: ideditalprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idedital
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__edital')
                field: idedital
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__editalrelatorio
    description: >
      Tabela responsável por armazenar relatórios exigidos pelos editais, com datas,
      aprovação e comprovações. Camada bronze tipada a partir de
      salic_bronze.sac__editalrelatorio.
    columns:
      - name: idrelatorio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idedital
        description: >
          Sequêncial identificador do edital.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__editalparcelas')
                field: idedital
      - name: nrparcela
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__editalparcelas')
                field: nrparcela
      - name: dtrelatorio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: relatorio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: comprovado
        description: >
          0.Não;1.Sim
        data_type: boolean
      - name: aprovado
        description: >
          0.Não;1.Sim
        data_type: boolean
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__efeitosresultado
    description: >
      Tabela responsável por armazenar códigos, opções e descrições de efeitos de resultado
      dos projetos. Camada bronze tipada a partir de salic_bronze.sac__efeitosresultado.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__empenho
    description: >
      Tabela responsável por registrar empenhos realizados, contendo UG, PT, natureza de
      despesa, data e valor. Camada bronze tipada a partir de salic_bronze.sac__empenho.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: uggestao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ugr
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ptres
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: programatrabalho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__fonte')
                field: codigo
      - name: naturezadespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__empenhoprojeto
    description: >
      Tabela utilizada para vincular empenhos aos projetos e convênios correspondentes.
      Camada bronze tipada a partir de salic_bronze.sac__empenhoprojeto.
    columns:
      - name: codigoempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__empenho')
                field: contador
      - name: codigoconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__convenio')
                field: contador
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__enquadramento
    description: >
      Tabela responsável por armazenar o enquadramento dos projetos, registrando data, tipo,
      observações e PRONAC. Camada bronze tipada a partir de salic_bronze.sac__enquadramento.
    columns:
      - name: idenquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__entregaproduto
    description: >
      Tabela responsável por registrar a entrega de produtos dos projetos, com datas e
      observações. Camada bronze tipada a partir de salic_bronze.sac__entregaproduto.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtentrega
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__estados
    description: >
      Tabela utilizada para armazenar estados ou fases do fluxo operacional do sistema, com
      indicação de próximo passo. Camada bronze tipada a partir de salic_bronze.sac__estados.
    columns:
      - name: id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: fluxoid
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__fluxos')
                field: id
      - name: proximo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__etiqueta
    description: >
      Tabela responsável por armazenar etiquetas relacionadas a ano, mecanismo e sequencial.
      Camada bronze tipada a partir de salic_bronze.sac__etiqueta.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__evolucao
    description: >
      Tabela responsável por registrar a evolução dos projetos, com fases, datas e
      informações relevantes. Camada bronze tipada a partir de salic_bronze.sac__evolucao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__fase')
                field: codigo
      - name: dtinicio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dttermino
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: infrelevantes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__fase
    description: >
      Tabela responsável por armazenar os tipos de fases utilizadas nos processos e fluxos
      dos projetos. Camada bronze tipada a partir de salic_bronze.sac__fase.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__fluxos
    description: >
      Tabela responsável por registrar os fluxos operacionais do sistema, contendo
      identificadores e descrições. Camada bronze tipada a partir de
      salic_bronze.sac__fluxos.
    columns:
      - name: id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__fluxosprojeto
    description: >
      Tabela responsável por armazenar o fluxo atual dos projetos, incluindo estado, órgão,
      grupo e agente responsável. Camada bronze tipada a partir de
      salic_bronze.sac__fluxosprojeto.
    columns:
      - name: id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: estadoid
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__fonte
    description: >
      Tabela responsável por registrar fontes de recurso ou classificação de origem de dados
      utilizadas no sistema. Camada bronze tipada a partir de salic_bronze.sac__fonte.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__genero
    description: >
      Tabela responsável por armazenar gêneros ou categorias utilizadas para classificar
      projetos ou ações culturais. Camada bronze tipada a partir de salic_bronze.sac__genero.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__geralext
    description: >
      Tabela responsável por consolidar informações gerais extraídas de pareceres, incluindo
      dados do projeto, análise técnica, proponente e valores. Camada bronze tipada a partir
      de salic_bronze.sac__geralext.
    columns:
      - name: nrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: projeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: sugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: processo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__grupoempresarial
    description: >
      Tabela responsável por registrar grupos empresariais, incluindo endereço, cidade, UF,
      país e representante. Camada bronze tipada a partir de
      salic_bronze.sac__grupoempresarial.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomegrupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: publica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: pais
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: presidente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__guiarecolhimento
    description: >
      Tabela responsável por armazenar informações de guias de recolhimento, com datas,
      valores, status e dados do proponente. Camada bronze tipada a partir de
      salic_bronze.sac__guiarecolhimento.
    columns:
      - name: numeroguia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__interessado')
                field: cgccpf
      - name: dtrecolhimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: statusguia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfunarte
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__historicoaspectosfisicosx
    description: >
      Tabela responsável por registrar o histórico dos aspectos físicos dos projetos, com
      itens, propostas e controle. Camada bronze tipada a partir de
      salic_bronze.sac__historicoaspectosfisicosx.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: itensfisicos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: proposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: prcont
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__historicoautuacaox
    description: >
      Tabela responsável por armazenar o histórico de autuação dos projetos, com datas,
      folhas e providências adotadas. Camada bronze tipada a partir de
      salic_bronze.sac__historicoautuacaox.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__autuacaox')
                field: anoprojeto
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__autuacaox')
                field: sequencial
      - name: dtautuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: folhas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: providencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__historicocarta
    description: >
      Tabela responsável por registrar o histórico de cartas emitidas, com datas, numeração,
      órgão e status. Camada bronze tipada a partir de salic_bronze.sac__historicocarta.
    columns:
      - name: idhistoricocarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: numerocarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__carta')
                field: codigo
      - name: dtcarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__historicodelecaoaprovacao
    description: >
      Tabela responsável por armazenar informações sobre deleções de aprovações, incluindo
      tipo, data, portaria e justificativas. Camada bronze tipada a partir de
      salic_bronze.sac__historicodelecaoaprovacao.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdelecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__historicoinabilitado
    description: >
      Tabela responsável por registrar o histórico de habilitação e inabilitação de
      proponentes, incluindo datas, órgão e situação. Camada bronze tipada a partir de
      salic_bronze.sac__historicoinabilitado.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dthabilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: habilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__historicosituacao
    description: >
      Tabela responsável por armazenar o histórico da situação dos projetos, incluindo
      providências tomadas e datas. Camada bronze tipada a partir de
      salic_bronze.sac__historicosituacao.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: providenciatomada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__historicounidade
    description: >
      Tabela responsável por registrar o histórico de movimentação dos projetos entre
      unidades de análise, com datas de entrada e saída. Camada bronze tipada a partir de
      salic_bronze.sac__historicounidade.
    columns:
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsaida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: unidadeanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtretorno
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__humanidades
    description: >
      Tabela responsável por armazenar informações de projetos de Humanidades, incluindo
      edição, tiragem, acervo, equipamentos e percentuais de patrocínio/doação. Camada bronze
      tipada a partir de salic_bronze.sac__humanidades.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tiragem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: edicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: periocidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nredicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: acervo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: equipamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pdpatrocinio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pddoacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pdcomercializacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idhumanidades
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__inabilitado
    description: >
      Tabela responsável por armazenar registros de inabilitação de proponentes ou projetos,
      com datas, órgão, tipo e situação de habilitação. Camada bronze tipada a partir de
      salic_bronze.sac__inabilitado.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: habilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idtipoinabilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtinabilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: date
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__incentivoporcgccpf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__incentivoporcgccpf.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__informacaotrimestralx
    description: >
      Tabela responsável por registrar informações trimestrais dos projetos do tipo X,
      incluindo dados físico-financeiros e observações. Camada bronze tipada a partir de
      salic_bronze.sac__informacaotrimestralx.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anorelatorio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: fisicofinanceiro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: trimestre1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: trimestre2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: trimestre3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: trimestre4
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__instrumentobanda
    description: >
      Tabela responsável por registrar os instrumentos utilizados por bandas dentro dos
      projetos, com códigos e opções de classificação. Camada bronze tipada a partir de
      salic_bronze.sac__instrumentobanda.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__interessado
    description: >
      Tabela responsável por armazenar dados completos de interessados vinculados ao sistema,
      incluindo contatos, endereço, natureza jurídica e grupo. Camada bronze tipada a partir
      de salic_bronze.sac__interessado.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: telefoneresidencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: telefonecomercial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: telefonecelular
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: telefonefax
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: natureza
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: esfera
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: administracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: utilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: responsavel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: enderecointernet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: correioeletronico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__internet
    description: >
      Tabela utilizada para armazenar informações estruturadas destinadas a operações via
      internet, contendo chaves, campos e valores configuráveis. Camada bronze tipada a
      partir de salic_bronze.sac__internet.
    columns:
      - name: tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: chavea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: chaveb
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: chavec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: chaved
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campob
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campoc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valora
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorb
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: valorc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valord
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valore
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__intranet
    description: >
      Tabela utilizada para armazenar informações estruturadas destinadas à intranet, com
      chaves, campos, quantidades, valores e datas de referência. Camada bronze tipada a
      partir de salic_bronze.sac__intranet.
    columns:
      - name: tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: chavea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: chaveb
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: chavec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: chaved
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: chavee
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: chavef
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campob
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campoc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campod
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campoe
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campof
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtdeb
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtdec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtded
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtdee
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtdef
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: valora
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorb
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valord
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valore
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: dataa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: datab
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: datac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: datad
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: datae
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dataf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__kitbanda
    description: >
      Tabela responsável por registrar kits de instrumentos e equipamentos de bandas,
      incluindo descrição, especificação, acabamento e preço unitário. Camada bronze tipada a
      partir de salic_bronze.sac__kitbanda.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: quantidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: kit
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: especificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: afinacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: acabamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: precounitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__kitentregue
    description: >
      Tabela responsável por armazenar informações sobre kits entregues aos projetos,
      incluindo datas, documentos e valores. Camada bronze tipada a partir de
      salic_bronze.sac__kitentregue.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: documento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtentrega
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: valorkit
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcomplianceitemcomprovado
    description: >
      Tabela responsável por registrar itens auditados, valores comprovados e diferenças.
      Camada bronze tipada a partir de salic_bronze.sac__tbcomplianceitemcomprovado.
    columns:
      - name: idcompliancefinanceiro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idcompliancecomprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrfonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iditem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idfornecedor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrlancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vllancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vldiferenca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsobservacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idcomprovantepagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcomprovacaopassivo
    description: >
      Tabela responsável por registrar comprovantes de passivos financeiros ou orçamentários.
      Camada bronze tipada a partir de salic_bronze.sac__tbcomprovacaopassivo.
    columns:
      - name: idcomprovacaopassivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaaprovacao')
                field: idplanilhaaprovacao
      - name: idlancamentobancario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tblancamentobancario')
                field: idlancamentobancario
      - name: idfornecedor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpcomprovante
        description: >
          1. Cupom Fiscal 2. Guia de Recolhimento 3. Nota Fiscal / Fatura 4. Recibo de
          Pagamento 5. RPA 6. Invoice
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtemissaocomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcomprovantetrimestral
    description: >
      Tabela responsável por registrar comprovantes trimestrais de execução física e
      financeira. Camada bronze tipada a partir de salic_bronze.sac__tbcomprovantetrimestral.
    columns:
      - name: idcomprovantetrimestral
        description: >
          Identifica o relatório trimestral entregue pelo proponente.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          Identiticador único de uma proposta de um projeto cultural no Ministério da Cultura
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcomprovante
        description: >
          Data em que foi cadastrado o relatório trimestral (comprovante).
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtinicioperiodo
        description: >
          Data inicial do período de cadastro do relatório trimestral. (Comprovante).
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimperiodo
        description: >
          Data do fim do período do relatório trimestral.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsetapasexecutadas
        description: >
          Descrição das etapas do projeto executado.
        data_type: text
      - name: dsacessibilidade
        description: >
          descrição das medidas de acessibilidade implantadas até o momento do relatório.
        data_type: text
      - name: dsdemocratizacaoacesso
        description: >
          descrição das medidas de democratização de acesso.
        data_type: text
      - name: dsimpactoambiental
        description: >
          Descrição das medidas efetivadas para não haver impacto ambiental na execução
          doprojeto.
        data_type: text
      - name: sicomprovantetrimestral
        description: >
          Situação dos comprovantes trimestrais: 1 - Salvo pelo proponente, 2 - Enviado pelo
          Proponente, 3 - Encaminhado pelo técnico de Acompanhamento, 4 - Em análise pelo
          técnico, 5 - Finalizado pelo técnico, 6 - Finalizado pelo coordenador.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcomprovantetrimestral
        description: >
          Número de ordem, a sequência do relatório trimestral.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idcadastrador
        description: >
          É o usuário logado no sistema do lado do proponente (Usuário externo). O controle
          de acesso será feito através do banco controle de acesso na tabela SGCAcesso.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsparecertecnico
        description: >
          Descrição do parecer emitido pelo técnico de acompanhamento do Minc.
        data_type: text
      - name: dsrecomendacao
        description: >
          São as recomendações do técnico de acompanhamento ao proponente.
        data_type: text
      - name: idtecnicoavaliador
        description: >
          É o usuário logado no sistema do lado do Minc (Usuário interno). O controle de
          acesso será feito através do banco Tabelas tabela Usuarios.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcomunicados
    description: >
      Tabela responsável por armazenar comunicados e avisos publicados no sistema. Camada
      bronze tipada a partir de salic_bronze.sac__tbcomunicados.
    columns:
      - name: idcomunicado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbcomunicados')
                field: idcomunicado
      - name: comunicado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stopcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Desativado;1.Ativado
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciovigencia
        description: >
          Data de inicio da exibição do comunicado
        data_type: timestamp
      - name: dtterminovigencia
        description: >
          Data de termino da exibição do comunicado
        data_type: timestamp
      - name: idedital
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__edital')
                field: idedital
      - name: sifavoritar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
      - name: idimagem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbconfigurarpagamento
    description: >
      Tabela responsável por registrar configurações de pagamento, despachos e vigências.
      Camada bronze tipada a partir de salic_bronze.sac__tbconfigurarpagamento.
    columns:
      - name: idconfigurarpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrdespachoinicial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdespachofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtconfiguracaopagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Histórico;1.Atual
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          0.Histórico;1.Atual
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbconfigurarpagamentoxtbassinantes
    description: >
      Tabela responsável por associar configurações de pagamento aos assinantes. Camada
      bronze tipada a partir de salic_bronze.sac__tbconfigurarpagamentoxtbassinantes.
    columns:
      - name: idconfigurarpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbconfigurarpagamento')
                field: idconfigurarpagamento
      - name: idassinantes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrordenacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcontabancariabloqueada
    description: >
      Tabela responsável por registrar contas bancárias bloqueadas ou liberadas e seus
      motivos. Camada bronze tipada a partir de salic_bronze.sac__tbcontabancariabloqueada.
    columns:
      - name: idcontabancariabloqueada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtliberacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtbloqueio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpbloqueio
        description: >
          0. Sanção, 1. Projeto finalizado
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuarioliberouconta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlliberado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcontacartao
    description: >
      Tabela responsável por registrar informações de cartões vinculados às contas dos
      projetos. Camada bronze tipada a partir de salic_bronze.sac__tbcontacartao.
    columns:
      - name: idcontacartao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: idcontabancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - relationships:
              arguments:
                to: ref('sac__contabancaria')
                field: idcontabancaria
      - name: nrcontacartao
        description: >
          Nº do cartão
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: nrcentrodecusto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrcpfportadordocartao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: cdproponentenobanco
        description: >
          Código do banco para identificar o proponente
        data_type: text
      - name: cdportadorcartaonobanco
        description: >
          Código do banco para identificar o portador do cartão de pagamento
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: stestado
        description: >
          0.Inativo; 1.Ativo
        data_type: boolean
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcovid
    description: >
      Tabela responsável por registrar procedimentos e ações realizadas no período de
      pandemia nos projetos. Camada bronze tipada a partir de salic_bronze.sac__tbcovid.
    columns:
      - name: idcovid
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpprocedimento
        description: >
          0.Prorogacao; 1. Liberação de conta
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtprocedimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsmotivacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsalcancedaacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcredenciaparecerista_2020
    description: >
      Tabela responsável por armazenar os registros de credenciamento de pareceristas no ano
      de 2020, incluindo unidade, área, segmento e status de atividade. Camada bronze tipada
      a partir de salic_bronze.sac__tbcredenciaparecerista_2020.
    columns:
      - name: idcredenciamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcumprimentoobjeto
    description: >
      Tabela responsável por registrar o cumprimento do objeto pelos projetos, incluindo
      etapas concluídas, acessibilidade, fruição, geração de empregos e conclusão técnica.
      Camada bronze tipada a partir de salic_bronze.sac__tbcumprimentoobjeto.
    columns:
      - name: idcumprimentoobjeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcadastro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsetapasconcluidas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsmedidasacessibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsmedidasfruicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsmedidaspreventivas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsinformacaoadicional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsorientacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsconclusao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stresultadoavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idusuariocadastrador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idtecnicoavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sicumprimentoobjeto
        description: >
          1 - Salvo pelo proponente; 2 - Enviado pelo proponente; 3 - Encaminhado pelo
          Técnico de Acompanhamento; 4 - Em análise pelo Técnico; 5 - Finalizado pelo
          Técnico; 6 - Finalizado pelo Coordenador.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idchefiaimediata
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtempregosdiretos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtempregosindiretos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsgeracaoempregos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtenviodaprestacaocontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsmetadadosavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcumprimentoobjetoxarquivo
    description: >
      Tabela responsável por vincular arquivos/documentos às informações de cumprimento de
      objeto. Camada bronze tipada a partir de salic_bronze.sac__tbcumprimentoobjetoxarquivo.
    columns:
      - name: idcumprimentoobjetoxarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idcumprimentoobjeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbcumprimentoobjeto')
                field: idcumprimentoobjeto
      - name: idarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idposicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbcustosvinculados
    description: >
      Tabela responsável por armazenar custos vinculados a itens de planilha dos projetos,
      incluindo observações e percentual de cálculo. Camada bronze tipada a partir de
      salic_bronze.sac__tbcustosvinculados.
    columns:
      - name: idcustosvinculados
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcadastro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsobservacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: pccalculo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdeclaracaobeneficiosfiscais
    description: >
      Tabela responsável por armazenar declarações de benefícios fiscais, com dados de
      proponente, doador, contribuições e base legal. Camada bronze tipada a partir de
      salic_bronze.sac__tbdeclaracaobeneficiosfiscais.
    columns:
      - name: iddeclaracaobeneficiosfiscais
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrexercicio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nranocalendario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpdeclaracao
        description: >
          O. Declaração Original; R. Declaração Retificadora
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcpfcnpjproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          Número do PRONAC
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcpfcnpjdoadorpatrocinador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdbaselegal
        description: >
          Enquadramento: 3. Artigo 18; 4 Artigo 26
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpcontribuicao
        description: >
          Tipo de apoio 0. Doação/Pattrocínio 1. Doação 2. Patrocínio 4. Contribuição ao FNC
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcontribuicao
        description: >
          Valor do incentivo
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdemandasdeorgaosdecontrole
    description: >
      Tabela responsável por registrar demandas recebidas de órgãos de controle, incluindo
      documentos, prazos e informações de resposta. Camada bronze tipada a partir de
      salic_bronze.sac__tbdemandasdeorgaosdecontrole.
    columns:
      - name: iddemandasdeorgaosdecontrole
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdemandasdeorgaosdecontrole')
                field: iddemandasdeorgaosdecontrole
      - name: cdsolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsdocumentosolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrseisolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrprocesso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sidemanda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dssolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrdiasresponder
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtfinalresposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrseiresposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stpriorizar
        description: >
          0.Não; 1.Sim
        data_type: boolean
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdemandasdeorgaosdecontrolexprojetos
    description: >
      Tabela responsável por relacionar demandas de órgãos de controle aos respectivos
      projetos PRONAC. Camada bronze tipada a partir de
      salic_bronze.sac__tbdemandasdeorgaosdecontrolexprojetos.
    columns:
      - name: iddemandasdeorgaosdecontrole
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdemandasdeorgaosdecontrole')
                field: iddemandasdeorgaosdecontrole
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdeslocamento
    description: >
      Tabela responsável por registrar deslocamentos nacionais ou internacionais relacionados
      ao projeto, com origem, destino e quantidades. Camada bronze tipada a partir de
      salic_bronze.sac__tbdeslocamento.
    columns:
      - name: iddeslocamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpaisorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iduforigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipioorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpaisdestino
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idufdestino
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipiodestino
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtde
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdespacho
    description: >
      Tabela responsável por armazenar despachos emitidos no processo, incluindo tipo, data,
      estado e encaminhamentos. Camada bronze tipada a partir de
      salic_bronze.sac__tbdespacho.
    columns:
      - name: iddespacho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: data
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: despacho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Atual;1.Historico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stencaminhamento
        description: >
          onde 1 para artigo 26 e 2 para artigo 18
        data_type: text
      - name: sidespacho
        description: >
          (P)endente;(F)inalizado
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdetalhaplanodistribuicao
    description: >
      Tabela responsável por detalhar o plano de distribuição dos produtos culturais,
      incluindo quantidades, preços, tipos e destinos. Camada bronze tipada a partir de
      salic_bronze.sac__tbdetalhaplanodistribuicao.
    columns:
      - name: iddetalhaplanodistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idplanodistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stdistribuicao
        description: >
          0.Distribuição totalmente gratuíta; 1. Venda de produtos
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtexemplares
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtgratuitadivulgacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtgratuitapatrocinador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtgratuitapopulacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpopularintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpopularparcial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitariopopularintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitapopularintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitapopularparcial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtproponenteintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtproponenteparcial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitarioproponenteintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitaproponenteintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitaproponenteparcial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitaprevista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: tplocal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpespaco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpvenda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdetalhaplanodistribuicaoreadequacao
    description: >
      Tabela responsável por armazenar detalhamentos do plano de distribuição após
      readequações, vinculando UF, município, produtos e receitas. Camada bronze tipada a
      partir de salic_bronze.sac__tbdetalhaplanodistribuicaoreadequacao.
    columns:
      - name: iddetalhaplanodistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idplanodistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtexemplares
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtgratuitadivulgacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtgratuitapatrocinador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtgratuitapopulacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpopularintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpopularparcial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitariopopularintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitapopularintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitapopularparcial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtproponenteintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtproponenteparcial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitarioproponenteintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitaproponenteintegral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitaproponenteparcial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreceitaprevista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: tplocal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpespaco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpvenda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddetalhaoriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdiligencia
    description: >
      Tabela responsável por registrar diligências emitidas aos projetos, seus prazos,
      produtos, documentos exigidos e situação. Camada bronze tipada a partir de
      salic_bronze.sac__tbdiligencia.
    columns:
      - name: iddiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtresposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: resposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stestado
        description: >
          0.Atual;1.Historico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanodistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idcodigodocumentosexigidos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__documentosexigidos')
                field: codigo
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stprorrogacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stenviado
        description: >
          S.Enviado; N.Não enviado
        data_type: text
      - name: cdsituacaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdiligenciaxarquivo
    description: >
      Tabela utilizada para vincular arquivos às diligências. Camada bronze tipada a partir
      de salic_bronze.sac__tbdiligenciaxarquivo.
    columns:
      - name: iddiligencia
        description: >
          Faz referencia à tabela tbDiligencia do banco SAC.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdiligencia')
                field: iddiligencia
      - name: idarquivo
        description: >
          Faz referencia à tabela tbArquivo do BDCORPORATIVO. O controle de integridade será
          feita pela aplicação.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdispositivomovel
    description: >
      Tabela responsável por armazenar registros de dispositivos móveis utilizados para
      acesso ao sistema (push notifications e autenticações). Camada bronze tipada a partir
      de salic_bronze.sac__tbdispositivomovel.
    columns:
      - name: idregistration
        description: >
          Código do dispositivo cadastrado na google ou apple
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtregistration
        description: >
          Data do registro do dispositivo cadastrado na google ou apple
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcpf
        description: >
          CPF do usuário logado
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: dtacesso
        description: >
          Data do último acesso ao aplicativo
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddispositivomovel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdistribuirparecer
    description: >
      Tabela responsável por distribuir projetos/produtos para pareceristas, com datas,
      órgãos, análises e status. Camada bronze tipada a partir de
      salic_bronze.sac__tbdistribuirparecer.
    columns:
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          0.Enquadramento ou idProduto p/Análise de Custo
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoanalise
        description: >
          0.Análise de Conteúdo;1.Análise de Custo;2.Análise de Custo Administrativo
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stestado
        description: >
          0.Atual;1.Histórico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: stprincipal
        description: >
          0.Não;1.Sim
        data_type: boolean
      - name: fecharanalise
        description: >
          0.Análise aberta; 1.Análise Fechada
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtretorno
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stdiligenciado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sianalise
        description: >
          0. Aguardando Distribuição para o parecerista (Coordenador de Parecer) 1. Em
          análise pelo parecerista (Parecerista) 2. Analisado pelo parecerista (Parecerista)
          3. Finalizado análise técnica (Parecerista) 4. Em validação pelo coordenador
          (Coordenador de Parecer) 5. Validados pelo coordenador (Coordenador de Parecer)
        data_type: integer
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdistribuirprojeto
    description: >
      Tabela responsável por distribuir projetos para avaliadores internos, incluindo envio,
      devolução, fechamento e estado. Camada bronze tipada a partir de
      salic_bronze.sac__tbdistribuirprojeto.
    columns:
      - name: iddistribuirprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tpdistribuicao
        description: >
          A.Recurso
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliador
        description: >
          Código do responsável pela análise.
        data_type: integer
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsobservacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stfecharanalise
        description: >
          0.Em Análise;1.Análise concluída
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfechamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stestado
        description: >
          0.Atual;1.Histórico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdistribuirprojetopassivo
    description: >
      Tabela utilizada para distribuir passivos de projetos para análise técnica, registrando
      recebimento, devolução e justificativas. Camada bronze tipada a partir de
      salic_bronze.sac__tbdistribuirprojetopassivo.
    columns:
      - name: iddistribuirprojetopassivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrecebimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtrestituicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          1. Encaminhado para análise; 0.Devolvido após análise
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdistribuirreadequacao
    description: >
      Tabela responsável por distribuir readequações para avaliadores e coordenadores, com
      datas, status e orientações. Camada bronze tipada a partir de
      salic_bronze.sac__tbdistribuirreadequacao.
    columns:
      - name: iddistribuirreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtenvioavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsorientacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtretornoavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stvalidacaocoordenador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtvalidacaocoordenador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idcoordenador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdocumento20100107_1x
    description: >
      Tabela histórica contendo documentos registrados em layout anterior (versão 1X de
      2010). Camada bronze tipada a partir de salic_bronze.sac__tbdocumento20100107_1x.
    columns:
      - name: iddocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: imdocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipodocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: noarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: taarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuariojuntada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtjuntada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idunidadecadastro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: codigocorreio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbdocumentoassinatura
    description: >
      Tabela responsável por armazenar documentos assinados digitalmente, vinculados ao ato
      administrativo. Camada bronze tipada a partir de
      salic_bronze.sac__tbdocumentoassinatura.
    columns:
      - name: iddocumentoassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipodoatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: conteudo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dt_criacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idcriadordocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacao
        description: >
          1 - Aberto - Disponível para assinatura 2 - Finalizado - Fechado para assinatura
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idatodegestao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbfiscalizacao
    description: >
      Tabela responsável por registrar as fiscalizações realizadas nos projetos, incluindo
      datas de início e fim, demandante, situação e responsáveis internos. Camada bronze
      tipada a partir de salic_bronze.sac__tbfiscalizacao.
    columns:
      - name: idfiscalizacao
        description: >
          Identificador sequencial de fiscalização de determinados projetos culturais.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          Identificador único de projetos cultural no Ministério da Cultura, faz referencia a
          tabela Projetos.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciofiscalizacaoprojeto
        description: >
          Data de inicio da fiscalização de um determinado projeto cultural.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimfiscalizacaoprojeto
        description: >
          Data final da fiscalização de um determinado projeto cultural.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrespostasolicitada
        description: >
          Data para posicionar o período de resposta da fiscalização.
        data_type: timestamp
      - name: dsfiscalizacaoprojeto
        description: >
          Descrição de informações relativas aos projetos que será fiscalizado.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpdemandante
        description: >
          Tipo do demandante responsável pela realização da fiscalização: 0 - SEFIC 1 - SAV
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stfiscalizacaoprojeto
        description: >
          Situação da fiscalização. 0 - Fiscalização em andamento 1 - Fiscalização em atraso
          2 - Fiscalização concluída para parecer.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          Identificador do agente responsável para execução da fiscalização, faz referência a
          tabela Agentes do banco Agentes, controle de integridade referencial via aplicação.
        data_type: integer
      - name: idsolicitante
        description: >
          Identificador do solicitante da fiscalização, faz referência a tabela Agentes do
          banco Agentes, controle de integridade referencial via aplicação.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuariointerno
        description: >
          Identifica o usuário que é funcionário do Minc na fiscalização. Faz referência ao
          atributo identificador usu_codigo da tabela Usuarios do banco Tabelas.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbfundopatrimonial
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__tbfundopatrimonial.
    columns:
      - name: idfundopatrimonial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmfundopatrimonial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: dsfundopatrimonial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdusuariogestor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idagenteexecutor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cpfcnpjexecutor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: nmexecutor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: vlfundopatrimonial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: dtcriacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtatualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdusuariocriacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cdusuarioatualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idarea
        description: >
          ID da área cultural do fundo patrimonial (referência para SAC.dbo.Area)
        data_type: integer
      - name: idcontacorrente
        description: >
          Referência para a conta corrente do fundo patrimonial (Agentes.dbo.ContaCorrente)
        data_type: integer
      - name: cpfcnpjfundo
        description: >
          CNPJ do fundo patrimonial (14 dígitos)
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbfundopatrimonialusuario
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__tbfundopatrimonialusuario.
    columns:
      - name: idfundopatrimonialusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idfundopatrimonial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbfundopatrimonial')
                field: idfundopatrimonial
      - name: idagenteapoiado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cpfcnpjapoiado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: nmapoiado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: vlvalordestinado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: dtvinculacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdesvinculacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stvinculacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsobservacoes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdusuariocriacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cdusuarioatualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbgerarpagamentoparecerista
    description: >
      Tabela responsável por armazenar os registros de geração de pagamento de pareceristas,
      com despacho, ordem bancária, datas e valor total. Camada bronze tipada a partir de
      salic_bronze.sac__tbgerarpagamentoparecerista.
    columns:
      - name: idgerarpagamentoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idconfigurarpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbconfigurarpagamento')
                field: idconfigurarpagamento
      - name: dtgeracaopagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtefetivacaopagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrdespacho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sipagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vltotalpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbhistoricoalteracaodocumento
    description: >
      Tabela responsável por registrar o histórico de alterações realizadas em documentos
      vinculados a projetos. Camada bronze tipada a partir de
      salic_bronze.sac__tbhistoricoalteracaodocumento.
    columns:
      - name: idhistoricoalteracaodocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: iddocumento
        description: >
          Identificador do documento anexado.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idhistoricoalteracaoprojeto
        description: >
          Identificador do historico de alteração do projeto.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbhistoricoalteracaoprojeto')
                field: idhistoricoalteracaoprojeto
      - name: iddocumentosexigidos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__documentosexigidos')
                field: codigo
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbhistoricoalteracaoprojeto
    description: >
      Tabela responsável por registrar o histórico de alterações de dados dos projetos (área,
      segmento, situação, órgão, execução, enquadramento e providências). Camada bronze
      tipada a partir de salic_bronze.sac__tbhistoricoalteracaoprojeto.
    columns:
      - name: idhistoricoalteracaoprojeto
        description: >
          Identificador do historico de alterações do projeto.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: cdarea
        description: >
          Áreas de seguimento do projeto: 1 Artes Cênicas 2 Audiovisual 3 Música 4 Artes
          Visuais 5 Patrimônio Cultural 6 Humanidades 7 Artes Integradas
        data_type: text
      - name: cdsegmento
        description: >
          Código do seguimento do projeto: 11 Teatro 12 Dança 13 Circo 14 Ópera 15 Mímica 16
          Ópera Rock 22 Videofonográfica 24 Rádio/Tvs Educativa 25 Multimídia, etc.
        data_type: text
      - name: nmprojeto
        description: >
          Armazena o nome do projeto.
        data_type: text
      - name: cdsituacao
        description: >
          Código da situação do projeto, registrado na tabela Situação.
        data_type: text
      - name: cdorgao
        description: >
          Unidade por onde o projeto é tramitado.
        data_type: integer
      - name: dtinicioexecucao
        description: >
          Data de inicio de execução de projeto.
        data_type: timestamp
      - name: dtfimexecucao
        description: >
          Data fim da execução do projeto.
        data_type: timestamp
      - name: idlogon
        description: >
          Usuário logado responsável pela alteração do registro.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddocumento
        description: >
          Identifica o documento anexado para alteração do registro.
        data_type: integer
      - name: idpronac
        description: >
          Identificador do projeto.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idenquadramento
        description: >
          Identificador do enquadramento utilizado para o projeto.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__enquadramento')
                field: idenquadramento
      - name: dthistoricoalteracaoprojeto
        description: >
          Armazena a data que foi realizada a alteração no projeto
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dshistoricoalteracaoprojeto
        description: >
          Descreve o motivo de alteração do projeto.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          numero do cgc ou cpf alterado
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: dsprovidenciatomada
        description: >
          Descrição da Providência tomada para alteração do registro
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbhistoricodevolucaofiscalizacao
    description: >
      Tabela responsável por registrar devoluções de relatórios de fiscalização, com
      justificativas, datas e status. Camada bronze tipada a partir de
      salic_bronze.sac__tbhistoricodevolucaofiscalizacao.
    columns:
      - name: idhistoricodevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idrelatoriofiscalizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativadevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviodevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: stdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbhistoricodiligencia
    description: >
      Tabela responsável por registrar o histórico de solicitações e respostas de
      diligências, com datas e usuários envolvidos. Camada bronze tipada a partir de
      salic_bronze.sac__tbhistoricodiligencia.
    columns:
      - name: idhistoricodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: iddiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdiligencia')
                field: iddiligencia
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtresposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: resposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbhistoricodocumento
    description: >
      Tabela responsável por registrar o histórico de tramitação de documentos entre
      unidades, incluindo envio, recebimento, ação e justificativa. Camada bronze tipada a
      partir de salic_bronze.sac__tbhistoricodocumento.
    columns:
      - name: idhistorico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dttramitacaoenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idusuarioemissor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: medespacho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idlote
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tblote')
                field: idlote
      - name: dttramitacaorecebida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idusuarioreceptor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: acao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbhistoricoemail
    description: >
      Tabela responsável por registrar o histórico de e-mails relacionados a projetos,
      propostas e textos padrão enviados pelo sistema. Camada bronze tipada a partir de
      salic_bronze.sac__tbhistoricoemail.
    columns:
      - name: idhistoricoemail
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idavaliacaoproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idtextoemail
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: dtemail
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Não enviado; 1.Enviado
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbhistoricoexclusaoconta
    description: >
      Tabela responsável por registrar exclusões de contas bancárias de projetos, com dados
      da conta, motivo, data e usuário. Camada bronze tipada a partir de
      salic_bronze.sac__tbhistoricoexclusaoconta.
    columns:
      - name: idhistoricoexclusaoconta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idcontabancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__contabancaria')
                field: idcontabancaria
      - name: banco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: contabloqueada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: contalivre
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtexclusao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: motivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbhomologacao
    description: >
      Tabela responsável por registrar as homologações de decisões relativas aos projetos,
      com tipo, data, decisão, descrição e usuário. Camada bronze tipada a partir de
      salic_bronze.sac__tbhomologacao.
    columns:
      - name: idhomologacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tphomologacao
        description: >
          1. Homologação normal; 2.Homologação por Ad Referedum
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dthomologacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: stdecisao
        description: >
          1.Desfavoravel; 2.Favorável
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dshomologacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbinadimplente
    description: >
      Tabela responsável por registrar situações de inadimplência e adimplência de
      proponentes e projetos, com datas, documentos e posicionamento. Camada bronze tipada a
      partir de salic_bronze.sac__tbinadimplente.
    columns:
      - name: idinadimplente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrcnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinadimplencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacaoanteriorinadimplencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsprovidenciaanteriorinadimplencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idusuarioefetivouindimplencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtadimplencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: iddocumentoanexado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idusuarioliberouinadimplencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stposicionamento
        description: >
          0. Adimplente; 1. Inadimplente
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Historico; 1.Ativo
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbitensplanilhaproduto
    description: >
      Tabela responsável por relacionar produtos aos itens e etapas da planilha,
      vinculando-os ao usuário que cadastrou. Camada bronze tipada a partir de
      salic_bronze.sac__tbitensplanilhaproduto.
    columns:
      - name: iditensplanilhaproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaetapa')
                field: idplanilhaetapa
      - name: idplanilhaitens
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaitens')
                field: idplanilhaitens
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tblancamentobancario
    description: >
      Tabela responsável por registrar lançamentos bancários associados aos projetos, com
      dados de agência, conta, valores e datas. Camada bronze tipada a partir de
      salic_bronze.sac__tblancamentobancario.
    columns:
      - name: idlancamentobancario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmovimentacaobancariaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbmovimentacaobancariaitem')
                field: idmovimentacaobancariaitem
      - name: stcontalancamento
        description: >
          0.Conta Captação;1.Conta Movimento
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: nragencialancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcontalancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdlancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dslancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrlancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtlancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vllancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: stlancamento
        description: >
          (D)ÉBITO OU (C)RÉDITO DO MOVIMENTO
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tblaudofinal
    description: >
      Tabela responsável por armazenar laudos finais do projeto, contendo manifestação, data,
      descrição e usuário responsável. Camada bronze tipada a partir de
      salic_bronze.sac__tblaudofinal.
    columns:
      - name: idlaudofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtlaudofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: simanifestacao
        description: >
          Manisfetação: A - Aprovado; P - Aprovado com ressalvas; R - Reprovado
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dslaudofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tblogintentativasacesso
    description: >
      Tabela responsável por registrar tentativas de login, com CPF, IP, data e quantidade de
      tentativas. Camada bronze tipada a partir de salic_bronze.sac__tblogintentativasacesso.
    columns:
      - name: nrcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrip
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dttentativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrtentativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idlogintentativasacesso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tblote
    description: >
      Tabela responsável por registrar lotes de processamento, com data de geração. Camada
      bronze tipada a partir de salic_bronze.sac__tblote.
    columns:
      - name: idlote
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtlote
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmanterportaria
    description: >
      Tabela responsável por armazenar portarias publicadas, com assinante, cargo, estado e
      descrição. Camada bronze tipada a partir de salic_bronze.sac__tbmanterportaria.
    columns:
      - name: idmanterportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsassinante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtportariapublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
      - name: dscargo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmensagem
    description: >
      Tabela responsável por registrar mensagens enviadas aos usuários, contendo título,
      descrição, datas e identificadores. Camada bronze tipada a partir de
      salic_bronze.sac__tbmensagem.
    columns:
      - name: idmensagem
        description: >
          Código da mensagem.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrcpf
        description: >
          Número do CPF do usuário logado.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          Código do projeto.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: titulo
        description: >
          Titulo da mensagem.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricao
        description: >
          Descrição da mensagem.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          Data em que a mensagem foi enviada.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtacesso
        description: >
          Data em que a mensagem acessada ou visualizada.
        data_type: timestamp
      - name: dtexclusao
        description: >
          Data da exclusão lógica.
        data_type: timestamp
      - name: idsuccess
        description: >
          Código da resposta da solicitação de envio de mensagem GCM.
        data_type: integer
      - name: idmulticast
        description: >
          Código unico da mensagem do serviço GCM.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmensagemdispositivomovel
    description: >
      Tabela responsável por relacionar mensagens aos dispositivos móveis vinculados aos
      usuários. Camada bronze tipada a partir de
      salic_bronze.sac__tbmensagemdispositivomovel.
    columns:
      - name: idmensagemdispositivomovel
        description: >
          Código da relação entre Mensagem e Dispositivo Móvel.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idmensagem
        description: >
          Código da mensagem.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbmensagem')
                field: idmensagem
      - name: iddispositivomovel
        description: >
          Código do dispositivo móvel.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdispositivomovel')
                field: iddispositivomovel
      - name: dtexclusao
        description: >
          Data da exclusão lógica.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmodelotermodecisao
    description: >
      Tabela responsável por armazenar modelos de termos de decisão utilizados no sistema.
      Camada bronze tipada a partir de salic_bronze.sac__tbmodelotermodecisao.
    columns:
      - name: idmodelotermodecisao
        description: >
          Identifica o modelo de termo de decisão.
        data_type: integer
      - name: idorgao
        description: >
          Orgão ao qual o modelo pertence.
        data_type: integer
      - name: idverificacao
        description: >
          fk com sac.dbo.Verificacao, com esse relacionamento há uma lista de tipos de modelo
          de termos possíveis.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: stmodelotermodecisao
        description: >
          Indica o status do modelo, sendo 1 para ativo e 0 para inativo.
        data_type: boolean
      - name: memodelotermodecisao
        description: >
          Conteúdo textual do modelo do termo de decisão.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmonitoramentoprojeto
    description: >
      Tabela responsável por registrar o monitoramento completo dos projetos, incluindo
      valores homologados, captados, utilizados e saldos. Camada bronze tipada a partir de
      salic_bronze.sac__tbmonitoramentoprojeto.
    columns:
      - name: idmonitoramentoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrmonitoramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpnotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtmonitoramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtliberacaoexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlhomologado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlreadequado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlperccaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlrecebidodeoutroprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vltransferidoparaoutroprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlperccomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldocontacaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldocontamovimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vltransferidocontamovimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlutilizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlpercutilizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vltotaldebitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: sisituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmotivodevolucao
    description: >
      Tabela responsável por registrar motivos de devolução relacionados a documentos
      assinados. Camada bronze tipada a partir de salic_bronze.sac__tbmotivodevolucao.
    columns:
      - name: idmotivodevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: iddocumentoassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdocumentoassinatura')
                field: iddocumentoassinatura
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dsmotivodevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmovimentacao
    description: >
      Tabela responsável por registrar movimentações gerais de projetos, com datas e estado.
      Camada bronze tipada a partir de salic_bronze.sac__tbmovimentacao.
    columns:
      - name: idmovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: movimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: dtmovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Atual;1.Histórico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmovimentacaobancaria
    description: >
      Tabela responsável por armazenar arquivos de movimentação bancária importados,
      incluindo período, banco e usuário. Camada bronze tipada a partir de
      salic_bronze.sac__tbmovimentacaobancaria.
    columns:
      - name: idmovimentacaobancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrbanco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtiniciomovimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtfimmovimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmovimentacaobancariaitem
    description: >
      Tabela responsável por registrar cada item de movimentação bancária, com saldos,
      valores, histórico, conta e data. Camada bronze tipada a partir de
      salic_bronze.sac__tbmovimentacaobancariaitem.
    columns:
      - name: idmovimentacaobancariaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tpregistro
        description: >
          0. Registro de saldo anterior;1.Registro de lançamento;2.Registro de Saldo Atual
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nragencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrconta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmtitulorazao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: nmabreviado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: dtaberturaconta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrcnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: vlsaldoinicial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: stsaldoinicial
        description: >
          (D)ÉBITO OU (C)RÉDITO
        data_type: text
      - name: vlsaldofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: stsaldofinal
        description: >
          (D)ÉBITO OU (C)RÉDITO
        data_type: text
      - name: dtmovimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: cdhistorico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dshistorico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrdocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlmovimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: stmovimento
        description: >
          (D)ÉBITO OU (C)RÉDITO
        data_type: text
      - name: idmovimentacaobancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbmovimentacaobancaria')
                field: idmovimentacaobancaria
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbmovimentacaobancariaitemxtipoinconsistencia
    description: >
      Tabela responsável por relacionar movimentações bancárias com tipos de inconsistências
      identificadas. Camada bronze tipada a partir de
      salic_bronze.sac__tbmovimentacaobancariaitemxtipoinconsistencia.
    columns:
      - name: idmovimentacaobancariaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbmovimentacaobancariaitem')
                field: idmovimentacaobancariaitem
      - name: idtipoinconsistencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbnormativos
    description: >
      Tabela responsável por armazenar normativos do sistema, com justificativas,
      comentários, datas de publicação, revogação e status. Camada bronze tipada a partir de
      salic_bronze.sac__tbnormativos.
    columns:
      - name: idnormativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmnormativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dscomentario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtpublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtrevogacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stestado
        description: >
          0.Historico; 1.Atual
        data_type: boolean
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbnotificacao
    description: >
      Tabela responsável por registrar notificações enviadas aos usuários e proponentes,
      incluindo datas, visualização e conteúdo. Camada bronze tipada a partir de
      salic_bronze.sac__tbnotificacao.
    columns:
      - name: idnotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpnotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsnotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtvisualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stvisualizacao
        description: >
          0.Não visualizado; 1.Visualizou
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Historico; 1.Atual
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbopinarprojeto
    description: >
      Tabela responsável por registrar opiniões, feedbacks e questionamentos feitos sobre
      projetos em avaliação. Camada bronze tipada a partir de
      salic_bronze.sac__tbopinarprojeto.
    columns:
      - name: idopinarprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idvisao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: sifaseprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtopiniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stquestionamento_1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stquestionamento_2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stquestionamento_3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dscomentario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsemail
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tborgaofiscalizador
    description: >
      Tabela responsável por vincular fiscalizações a órgãos fiscalizadores, contendo datas,
      observações e responsáveis. Camada bronze tipada a partir de
      salic_bronze.sac__tborgaofiscalizador.
    columns:
      - name: idorgaofiscalizador
        description: >
          Registros do órgãos responsáveis pela fiscalização
        data_type: integer
      - name: idorgao
        description: >
          Identificador do órgao que está sendo fiscalizado, faz referência a tabela órgão.
        data_type: integer
      - name: idfiscalizacao
        description: >
          Identificador da fiscalização, faz referência a tabela tbFiscalizacao.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbfiscalizacao')
                field: idfiscalizacao
      - name: dsobservacao
        description: >
          Observação informada pela unidade participante.
        data_type: text
      - name: dtrecebimentoresposta
        description: >
          Data de recebimento da resposta enviada pelo fiscal.
        data_type: timestamp
      - name: dtconfirmacaofiscalizacao
        description: >
          Data de confirmação da fiscalização.
        data_type: timestamp
      - name: idresponsavelconfirmacao
        description: >
          Identificador do responsável pela confirmação, referencia a tabela Agentes do banco
          Agentes, controle de integridade referencial via aplicação.
        data_type: integer
      - name: idparecerista
        description: >
          Identificador do parecerista do órgão fiscalizador, cada órgão fiscalizador poderá
          selecionar ou não, um parecerista conhecedor do projeto para acompanhar o fiscal
          responsável pela fiscalização, faz referência a tabela Agentes do banco Agentes,
          controle de integridade referencial via aplicação.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpagamentopareceristaxarquivo
    description: >
      Tabela responsável por vincular arquivos aos registros de pagamento de pareceristas.
      Camada bronze tipada a partir de salic_bronze.sac__tbpagamentopareceristaxarquivo.
    columns:
      - name: idgerarpagamentoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbgerarpagamentoparecerista')
                field: idgerarpagamentoparecerista
      - name: idarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: siarquivo
        description: >
          1.Enviado para assinatura;2.Assinado pelo parecerista
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpagarparecerista
    description: >
      Tabela responsável por armazenar pagamentos realizados a pareceristas, com valores,
      datas, unidade e empenho. Camada bronze tipada a partir de
      salic_bronze.sac__tbpagarparecerista.
    columns:
      - name: idpagarparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idnrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidadeanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idgerarpagamentoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbgerarpagamentoparecerista')
                field: idgerarpagamentoparecerista
      - name: vlpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dthomologacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idpareceristaempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbpareceristaempenho')
                field: idpareceristaempenho
      - name: nrempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpagarpareceristareadequacao
    description: >
      Tabela responsável por registrar pagamentos a pareceristas referentes a análises de
      readequação. Camada bronze tipada a partir de
      salic_bronze.sac__tbpagarpareceristareadequacao.
    columns:
      - name: idpagarreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dthomologacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idgerarpagamentoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbgerarpagamentoparecerista')
                field: idgerarpagamentoparecerista
      - name: vlpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idpareceristaempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbpareceristaempenho')
                field: idpareceristaempenho
      - name: nrempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpagarpareceristarecurso
    description: >
      Tabela responsável por registrar pagamentos a pareceristas referentes a análises de
      recursos. Camada bronze tipada a partir de salic_bronze.sac__tbpagarpareceristarecurso.
    columns:
      - name: idpagarrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tpsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dthomologacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idgerarpagamentoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbgerarpagamentoparecerista')
                field: idgerarpagamentoparecerista
      - name: vlpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idpareceristaempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbpareceristaempenho')
                field: idpareceristaempenho
      - name: nrempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpareceristaafastamento
    description: >
      Tabela responsável por registrar afastamentos de pareceristas, com motivos, datas,
      justificativas e anexos. Camada bronze tipada a partir de
      salic_bronze.sac__tbpareceristaafastamento.
    columns:
      - name: idpareceristaafastamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmotivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioafastamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimafastamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtautorizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpareceristaedital
    description: >
      Tabela responsável por registrar a participação de pareceristas em editais, incluindo
      vigência, credenciamento e estado. Camada bronze tipada a partir de
      salic_bronze.sac__tbpareceristaedital.
    columns:
      - name: nredital
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtiniciovigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimvigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtcredenciamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdescredenciamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stestado
        description: >
          0.Ativo; 1.Inativo
        data_type: boolean
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idpareceristasedital
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpareceristaempenho
    description: >
      Tabela responsável por armazenar empenhos emitidos a pareceristas, com valores, datas e
      situação. Camada bronze tipada a partir de salic_bronze.sac__tbpareceristaempenho.
    columns:
      - name: idpareceristaempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idinexigibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbpareceristainexigibilidade')
                field: idpareceristainexigibilidade
      - name: nrempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0. Ativo; 1.Histórico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpareceristainexigibilidade
    description: >
      Tabela responsável por registrar pagamentos ou procedimentos de inexigibilidade
      relacionados a pareceristas. Camada bronze tipada a partir de
      salic_bronze.sac__tbpareceristainexigibilidade.
    columns:
      - name: idpareceristainexigibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbpareceristainexigibilidade')
                field: idpareceristainexigibilidade
      - name: dtinexigibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlinexigibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Ativo; 1. Histórico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: identificacaoinex
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpareceristaprojetoredistribuido
    description: >
      Tabela responsável por registrar redistribuições de projetos entre pareceristas. Camada
      bronze tipada a partir de salic_bronze.sac__tbpareceristaprojetoredistribuido.
    columns:
      - name: idpareceristaprojetoredistribuido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtredistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalise
        description: >
          0. Análise inicial; 1.Recurso; 2.Readequação
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: simotivo
        description: >
          0.Declinado; 1. Retirado
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpareceristaprorrogarprazo
    description: >
      Tabela responsável por registrar prorrogações de prazo solicitadas por pareceristas
      durante análises. Camada bronze tipada a partir de
      salic_bronze.sac__tbpareceristaprorrogarprazo.
    columns:
      - name: idpareceristaprorrogarprazo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: iddistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtconcessao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpareceristaprorrogarprazoxdistribuicao
    description: >
      Tabela responsável por vincular prorrogações de prazo às distribuições de análises.
      Camada bronze tipada a partir de
      salic_bronze.sac__tbpareceristaprorrogarprazoxdistribuicao.
    columns:
      - name: idpareceristaprorrogarprazoxdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpareceristaprorrogarprazo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbpareceristaprorrogarprazo')
                field: idpareceristaprorrogarprazo
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: iddsitribuirreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirreadequacao')
                field: iddistribuirreadequacao
      - name: iddistribuirrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirprojeto')
                field: iddistribuirprojeto
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpareceristavaloranalisado
    description: >
      Tabela responsável por registrar valores analisados pelos pareceristas durante a etapa
      de avaliação. Camada bronze tipada a partir de
      salic_bronze.sac__tbpareceristavaloranalisado.
    columns:
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: date
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanilhaaprovacao
    description: >
      Tabela responsável por armazenar planilhas de aprovação orçamentária, contendo itens,
      valores, justificativas e vínculos com etapas. Camada bronze tipada a partir de
      salic_bronze.sac__tbplanilhaaprovacao.
    columns:
      - name: idplanilhaaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tpplanilha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtplanilha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaprojeto')
                field: idplanilhaprojeto
      - name: idplanilhaproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaproposta')
                field: idplanilhaproposta
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaetapa')
                field: idplanilhaetapa
      - name: idplanilhaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaitens')
                field: idplanilhaitens
      - name: dsitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaunidade')
                field: idunidade
      - name: qtitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpdespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tppessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcontrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrfonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idufdespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipiodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idplanilhaaprovacaopai
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaaprovacao')
                field: idplanilhaaprovacao
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: tpacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idrecursodecisao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stcustopraticado
        description: >
          0.Dentro do custo praticado; 1.Acima do custo praticado.
        data_type: boolean
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanilhaetapa
    description: >
      Tabela responsável por registrar etapas orçamentárias, incluindo descrição, tipo,
      ordenação e status. Camada bronze tipada a partir de salic_bronze.sac__tbplanilhaetapa.
    columns:
      - name: idplanilhaetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpcusto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stestado
        description: >
          0.Inativo;1.Ativo
        data_type: boolean
      - name: tpgrupo
        description: >
          Os tipo de grupo saão usandos para controlar os remanejamento de valores entre as
          etapas. O tipo A permite remanejamento entre as etapas 1 e 2. Os outros tipos
          somente dentro da etapa.
        data_type: text
      - name: nrordenacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanilhaitens
    description: >
      Tabela responsável por armazenar itens orçamentários cadastrados pelos usuários. Camada
      bronze tipada a partir de salic_bronze.sac__tbplanilhaitens.
    columns:
      - name: idplanilhaitens
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanilhajustificativa
    description: >
      Tabela responsável por registrar justificativas sugeridas e aprovadas dentro das
      planilhas de orçamento. Camada bronze tipada a partir de
      salic_bronze.sac__tbplanilhajustificativa.
    columns:
      - name: idplanilhaorcamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idplanilhaprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaprojeto')
                field: idplanilhaprojeto
      - name: data
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: vlsugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: operacaosugerida
        description: >
          0.Subtrair;1.Somar
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativasugerida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlaprovado
        description: >
          0
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: operacaoaprovada
        description: >
          0.Subtrair;1.Somar
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativaaprovada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Atual;1.Histórico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanilhaprojeto
    description: >
      Tabela responsável por armazenar a estrutura orçamentária aprovada para o projeto, com
      itens, valores e justificativas. Camada bronze tipada a partir de
      salic_bronze.sac__tbplanilhaprojeto.
    columns:
      - name: idplanilhaprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idplanilhaproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaproposta')
                field: idplanilhaproposta
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaetapa')
                field: idplanilhaetapa
      - name: idplanilhaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaitens')
                field: idplanilhaitens
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaunidade')
                field: idunidade
      - name: quantidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ufdespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipiodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stcustopraticado
        description: >
          0.Dentro do custo praticado; 1.Acima do custo praticado.
        data_type: boolean
      - name: tpacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanilhaproposta
    description: >
      Tabela responsável por registrar a planilha de orçamento proposta pelo proponente antes
      da aprovação. Camada bronze tipada a partir de salic_bronze.sac__tbplanilhaproposta.
    columns:
      - name: idplanilhaproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaetapa')
                field: idplanilhaetapa
      - name: idplanilhaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaitens')
                field: idplanilhaitens
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: unidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaunidade')
                field: idunidade
      - name: quantidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipodespesa
        description: >
          0.Não informado;1.Capital;2.Correntes
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          0.Não informado;1.Pessoa Fisica;2.Pessoa Juridica
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartida
        description: >
          0.Não informado;1.Não;2.Sim
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          0.Não informado;1.Federal-Lei 8313;2.Estadual;3.Municipal;4.Outros Recursos
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ufdespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipiodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stcustopraticado
        description: >
          0.Dentro do custo praticado; 1.Acima do custo praticado.
        data_type: boolean
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanilhaunidade
    description: >
      Tabela responsável por armazenar unidades de medida utilizadas nas planilhas de
      orçamento. Camada bronze tipada a partir de salic_bronze.sac__tbplanilhaunidade.
    columns:
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanodistribuicao
    description: >
      Tabela responsável por registrar o plano de distribuição de produtos culturais, com
      quantitativos, preços e regras de distribuição. Camada bronze tipada a partir de
      salic_bronze.sac__tbplanodistribuicao.
    columns:
      - name: idplanodistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__area')
                field: codigo
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idposicaologo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtproduzida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpatrocinador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtoutros
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtvendanormal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtvendapromocional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitarionormal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitariopromocional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalisetecnica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalisecomissao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: canalaberto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
      - name: qtdevendapopularnormal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtdevendapopularpromocional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: vlunitariopopularnormal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: precounitarionormal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: receitapopularpromocional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: receitapopularnormal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: vlreceitatotalprevista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: idplanodistribuicaooriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbplanodivulgacao
    description: >
      Tabela responsável por registrar o plano de divulgação do projeto, incluindo peças,
      veículos e análises. Camada bronze tipada a partir de
      salic_bronze.sac__tbplanodivulgacao.
    columns:
      - name: idplanodivulgacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: idpeca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idveiculo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalisetecnica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalisecomissao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpreprojetometa
    description: >
      Tabela responsável por registrar metas adicionais, atributos e histórico de alterações
      vinculados ao pré-projeto. Camada bronze tipada a partir de
      salic_bronze.sac__tbpreprojetometa.
    columns:
      - name: idpreprojetometa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: metakey
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: metavalue
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtalteracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbpreprojetoxenquadramento
    description: >
      Tabela responsável por registrar o estado de análise do enquadramento dos pré-projetos.
      Camada bronze tipada a partir de salic_bronze.sac__tbpreprojetoxenquadramento.
    columns:
      - name: idproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: siestadoanalise
        description: >
          0. Não avaliado; 1. Enquadramento pelo Componente da Comissão; 2. Enquadramento
          pelo Técnico; 3. Enquadramento pelo Coordenador; 4. Enquadramento pelo
          Coordenador-Geral
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprioridadedeanalise
    description: >
      Tabela responsável por registrar prioridades de análise dos projetos, incluindo
      motivação, tipo, urgência e responsável. Camada bronze tipada a partir de
      salic_bronze.sac__tbprioridadedeanalise.
    columns:
      - name: idprioridadedeanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtprioridade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dsmotivacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idnrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: siurgencia
        description: >
          0. Não urgente;1.Urgente
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalise
        description: >
          0. Análise Inicial; 1. Recurso
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Historico; 1.Atual
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprodutoxtipicidade
    description: >
      Tabela responsável por relacionar produtos às tipicidades e tipologias, vinculando
      área, segmento e indicação de principal. Camada bronze tipada a partir de
      salic_bronze.sac__tbprodutoxtipicidade.
    columns:
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprodutoxtipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanet
    description: >
      Tabela responsável por cadastrar programas Rouanet, com dados de período, objetivos,
      limites de proposta, valores e situação. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanet.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtabertura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfechamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsobjetivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtavaliadores
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtpropostas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tppessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
      - name: vlmaximoproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: dthabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtresultado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
      - name: dtselecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrpropostaselecionada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrpontuacaominima
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: vltotalprograma
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetareasegmento
    description: >
      Tabela responsável por vincular programas Rouanet às respectivas áreas e segmentos
      culturais. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetareasegmento.
    columns:
      - name: idprogramarouanetareasegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetavaliacao
    description: >
      Tabela responsável por registrar as avaliações dos avaliadores sobre as propostas do
      programa Rouanet, com pontuação e situação. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetavaliacao.
    columns:
      - name: idprogramarouanetavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idprogramarouanetpergunta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanetpergunta')
                field: idprogramarouanetpergunta
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpontos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: siavaliacao
        description: >
          0. Em avaliação; 1. avaliação finalizada
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetavaliacaorecurso
    description: >
      Tabela responsável por registrar as avaliações em fase de recurso, com médias de pontos
      e situação do recurso. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetavaliacaorecurso.
    columns:
      - name: idprogramarouanetavaliacaorecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idprogramarouanetpergunta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanetpergunta')
                field: idprogramarouanetpergunta
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtmediapontos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpontosrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: siavaliacaorecurso
        description: >
          0. Em avaliação; 1. avaliação finalizada
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetavaliador
    description: >
      Tabela responsável por relacionar avaliadores aos programas Rouanet, por área e
      segmento de atuação. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetavaliador.
    columns:
      - name: idprogramarouanetavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetdistribuirproposta
    description: >
      Tabela responsável por distribuir propostas de pré-projetos aos avaliadores do programa
      Rouanet, indicando tipo de análise e avaliação. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetdistribuirproposta.
    columns:
      - name: idprogramarouanetdistribuirproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalise
        description: >
          0.Avaliacao Inicial;1.Recurso
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpavaliacao
        description: >
          A. Proposta; R.Recurso
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanethabilitacao
    description: >
      Tabela responsável por registrar a habilitação das propostas no programa Rouanet, com
      datas, descrição e situação. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanethabilitacao.
    columns:
      - name: idprogramarouanethabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dthabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dshabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sihabilitacao
        description: >
          0. Inabilitado, 1.Habilitado
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetlocalidade
    description: >
      Tabela responsável por vincular programas Rouanet a UF e municípios atendidos. Camada
      bronze tipada a partir de salic_bronze.sac__tbprogramarouanetlocalidade.
    columns:
      - name: idprogramarouanetlocalidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetpergunta
    description: >
      Tabela responsável por cadastrar as perguntas/itens de avaliação do programa Rouanet,
      com faixas de pontuação e ordem. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetpergunta.
    columns:
      - name: idprogramarouanetpergunta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: dspergunta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrfaixainicio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrfaixaincremento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrfaixafinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrordem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetpropostaselecionada
    description: >
      Tabela responsável por registrar as propostas selecionadas no programa Rouanet, com
      dados de UF, região, pontuação, ranking e valores alocados. Camada bronze tipada a
      partir de salic_bronze.sac__tbprogramarouanetpropostaselecionada.
    columns:
      - name: idprogramarouanetpropostaselecionada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sguf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrrankingpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrrankingfinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: siselecao
        description: >
          0.Propostas suplementares incluídas para garantir equidade; 1. Propostas
          selecionadas inicialmente
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: siselecaoequidade
        description: >
          0.Propostas suplementares incluídas para garantir equidade; 1. Propostas
          selecionadas inicialmente
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlalocadoarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlalocadouf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlalocadoregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdfaixa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetrecurso
    description: >
      Tabela responsável por registrar recursos interpostos no âmbito do programa Rouanet,
      com datas, manifestações e situação. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetrecurso.
    columns:
      - name: idprogramarouanetrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: simanifestacao
        description: >
          0. Indeferido; 1. Deferido
        data_type: integer
      - name: dtmanifestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsmanifestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sirecurso
        description: >
          0.Cadastramento;1.Enviado;2.Em análise;3.Finalizado
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siestado
        description: >
          0.Historico; 1.Ativo
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetresultadofinal
    description: >
      Tabela responsável por consolidar o resultado final de avaliação das propostas do
      programa Rouanet, com pontuação total, ranking e situação. Camada bronze tipada a
      partir de salic_bronze.sac__tbprogramarouanetresultadofinal.
    columns:
      - name: idprogramarouanetresultadofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrtotalpontos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: mrpontosextras
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsmotivacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrranking
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: siresultadofinal
        description: >
          (S)elecionada;(D)esclassificada
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetresultadopreliminar
    description: >
      Tabela responsável por registrar o resultado preliminar de avaliação das propostas do
      programa Rouanet. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetresultadopreliminar.
    columns:
      - name: idprogramarouanetresultadopreliminar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: siresultadopreliminar
        description: >
          (C)lassficada; (D)esclassificada; (S)elecionada
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetselecaopatrocinador
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetselecaopatrocinador.
    columns:
      - name: idprogramarouanetselecaopatrocinador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpatrocinador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stopcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: siselecionado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprogramarouanetxproposta
    description: >
      Tabela responsável por relacionar propostas de pré-projeto aos programas Rouanet em que
      estão inscritas. Camada bronze tipada a partir de
      salic_bronze.sac__tbprogramarouanetxproposta.
    columns:
      - name: idprogramarouanetxproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprojetofase
    description: >
      Tabela responsável por registrar as fases de cada projeto, com datas de início, fim,
      normativos e estado. Camada bronze tipada a partir de salic_bronze.sac__tbprojetofase.
    columns:
      - name: idprojetofase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idnormativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbnormativos')
                field: idnormativo
      - name: idfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: dtiniciofase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfinalfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stestado
        description: >
          0.Histórico;1.Atual
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprojetoreadequacao
    description: >
      Tabela responsável por armazenar as informações textuais do projeto em contexto de
      readequação (resumo, objetivos, justificativas, etapas etc.). Camada bronze tipada a
      partir de salic_bronze.sac__tbprojetoreadequacao.
    columns:
      - name: idprojetoreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsresumodoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsobjetivos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsacessibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsdemocratizacaodeacesso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsetapadetrabalho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsfichatecnica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssinopse
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsimpactoambiental
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsespecificacaotecnica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsestrategiadeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsdescricaoatividade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprojetorecebedorrecurso
    description: >
      Tabela responsável por registrar projetos que recebem recursos de outros projetos, com
      valores, tipo de transferência e datas. Camada bronze tipada a partir de
      salic_bronze.sac__tbprojetorecebedorrecurso.
    columns:
      - name: idprojetorecebedorrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idsolicitacaotransferenciarecursos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronactransferidor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronacrecebedor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptransferencia
        description: >
          0.Não homologado; 1.Saldo Remanecente; 2. Transferência antes da emissão do cartão
          e do início da execução.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrecebimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlrecebido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprojetosclonados
    description: >
      Tabela responsável por registrar projetos e propostas clonadas, com referências ao
      projeto original, situação e valores. Camada bronze tipada a partir de
      salic_bronze.sac__tbprojetosclonados.
    columns:
      - name: idprojetosclonados
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpropostaclonada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtclonagrem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuarioclonador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronacoriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpropostaoriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpjcpforiginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacaoprojetooriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprocessooriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdareaoriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmentooriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsolicitadooriginal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbprojetosituacaoanterior
    description: >
      Tabela responsável por armazenar o histórico da situação anterior do projeto, com
      providências, órgão e usuário responsáveis. Camada bronze tipada a partir de
      salic_bronze.sac__tbprojetosituacaoanterior.
    columns:
      - name: idprojetosituacaoanterior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacaoanterior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacaoanterior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsprovidenciaanterior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaoanterior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuarioanterior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tpacao
        description: >
          Tipo de Ação: 1. Prorrogação automática; 2. Envio de diligência
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Atual; 1.Historico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbreadequacao
    description: >
      Tabela responsável por registrar solicitações de readequação de projetos, com
      justificativa, avaliação, reunião e situação. Camada bronze tipada a partir de
      salic_bronze.sac__tbreadequacao.
    columns:
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtiporeadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: iddocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: statendimento
        description: >
          Status de atendimento referente a solicitação do recurso: D : Deferido I :
          Indeferido N : Não avaliado; A : Em análise; E : Descartado
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: stanalise
        description: >
          AC : Aprovado pelo Componente; IC : Indeferido pelo Componente; AS : Aprovado pelo
          Plenário da CNIC; IS : Indeferido pelo Plenário da CNIC
        data_type: text
      - name: idnrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stestado
        description: >
          0.Atual;1.Histórico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbreadequacaoxparecer
    description: >
      Tabela responsável por relacionar readequações aos pareceres emitidos. Camada bronze
      tipada a partir de salic_bronze.sac__tbreadequacaoxparecer.
    columns:
      - name: idreadequacaoxparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: idparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbreadequacaoxtbdiligencia
    description: >
      Tabela responsável por relacionar readequações às diligências associadas. Camada bronze
      tipada a partir de salic_bronze.sac__tbreadequacaoxtbdiligencia.
    columns:
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: iddiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdiligencia')
                field: iddiligencia
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbrecurso
    description: >
      Tabela responsável por registrar recursos de projetos já aprovados/avaliados, com
      solicitação, avaliação, reunião e situação de análise. Camada bronze tipada a partir de
      salic_bronze.sac__tbrecurso.
    columns:
      - name: idrecurso
        description: >
          Chave artificial de identificação do recurso.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          Chave estrangeria, código de identificação do projeto.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsolicitacaorecurso
        description: >
          Data em que o recurso foi solicitado.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssolicitacaorecurso
        description: >
          Descrição do agente solicitante referente ao recurso.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagentesolicitante
        description: >
          Chave estrangeira, código de identificação do agente solicitante.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtavaliacao
        description: >
          Data em que o agente avaliador emitiu uma resposta referente ao recurso solicitado.
        data_type: timestamp
      - name: dsavaliacao
        description: >
          Descrição da análise feita pelo agente avaliador referente ao recurso solicitado.
        data_type: text
      - name: tprecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpsolicitacao
        description: >
          Tipo de solicitação: EN - Enquadramento; OR - Orçamento; PI - Projeto Indeferido;EO
          - Enquadramento e Orçamento; RE - Recurso fase edital; DR- Desistência do recurso
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagenteavaliador
        description: >
          Chave artificial, código de identificação do agente avaliador.
        data_type: integer
      - name: statendimento
        description: >
          Status de atendimento referente a solicitação do recurso: D - Deferido; I -
          Indeferido; N - Não avaliado
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sifaseprojeto
        description: >
          Fase 1 - Proposta; Fase 2 - Aprovacão; Fase 3 - Captação de recursos; Fase 4 -
          Execução; Fase 5 Prestação de contas
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sirecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stanalise
        description: >
          AC - Aprovado pelo comissário; IC - Indeferido pelo comissário; AS - Aprovado pela
          plenária; IS - Indeferido pela plenária
        data_type: text
      - name: idnrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stestado
        description: >
          0.Atual;1.Histórico
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__tbrecursoproposta
    description: >
      Tabela responsável por registrar recursos de propostas (pré-projetos), com manifestação
      do proponente e avaliação técnica. Camada bronze tipada a partir de
      salic_bronze.sac__tbrecursoproposta.
    columns:
      - name: idrecursoproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrecursoproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dsrecursoproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliadortecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtavaliacaotecnica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsavaliacaotecnica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tprecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: statendimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idarquivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: strascunho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcapmecaudioanoprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcapmecaudioanoprojeto.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcapmecaudioprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcapmecaudioprojeto.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcaptacaoanoufareasegmentosemestre
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcaptacaoanoufareasegmentosemestre.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: semestre_1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: semestre_2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: total
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcaptacaopormptipoapoiomecena
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcaptacaopormptipoapoiomecena.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mp
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoapoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcaptado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcaptado.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcaptadoano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcaptadoano.
    columns:
      - name: anorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: medidaprovisoria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcaptadoanomesdiaprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcaptadoanomesdiaprojeto.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcaptadoanoprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcaptadoanoprojeto.
    columns:
      - name: anorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcartaaudio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcartaaudio.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcartapropaudio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcartapropaudio.
    columns:
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcartas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcartas.
    columns:
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: numero
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: processo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: responsavel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: unidadeanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vchecaproponente
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vchecaproponente.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: quant
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vchecaqteinabilitacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vchecaqteinabilitacao.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: quant
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vci
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vci.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ci
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgaoexpedidor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtexpedicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcnpjcpf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcnpjcpf.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcnpjvalido
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcnpjvalido.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjvalido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcomparativoano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcomparativoano.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: q_apresentado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: t_apresentado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: q_aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: t_aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: q_captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: t_captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcomparativoanomec
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcomparativoanomec.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: q_apresentado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: t_apresentado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: q_aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: t_aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: q_captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: t_captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcontacorrente
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcontacorrente.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: banco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: contacorrente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: artigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vconvenioanoarea
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vconvenioanoarea.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorconveniol
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vconvenioanouf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vconvenioanouf.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vcustodoprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vcustodoprojeto.
    columns:
      - name: tipoprestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: custoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vdadoscomplementaresdecartas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vdadoscomplementaresdecartas.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: processo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: responsavel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtpublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: saldoacaptar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vdadosconvenio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vdadosconvenio.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciovigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimvigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: vlconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: contador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vdadosportariaaprovacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vdadosportariaaprovacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: numero
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtpublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vdiligencias
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vdiligencias.
    columns:
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: controladores
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: documentosolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdocumentosolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: posicionamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: constatacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: documentoresposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrdocumentoresposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtresposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: resposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vdocumentoexigido
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vdocumentoexigido.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: documentoexigido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vdtprimeiraultimacaptacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vdtprimeiraultimacaptacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtprimeira
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtultima
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__veiculacao
    description: >
      Tabela responsável por cadastrar tipos de veiculação. Camada bronze tipada a partir de
      salic_bronze.sac__veiculacao.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vempresasincentivadorasano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vempresasincentivadorasano.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: empresa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__verificacao
    description: >
      Tabela responsável por registrar verificações, tipos e estados associados. Camada
      bronze tipada a partir de salic_bronze.sac__verificacao.
    columns:
      - name: idverificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idtipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          0.Inativo;1.Ativo
        data_type: boolean
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__verificacaopecaxveiculo
    description: >
      Tabela responsável por relacionar peças e veículos em processos de verificação. Camada
      bronze tipada a partir de salic_bronze.sac__verificacaopecaxveiculo.
    columns:
      - name: idverificacaopecaxveiculo
        description: >
          Identificação da Peça vinculada ao veículo
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idverificacaopeca
        description: >
          Identificacao da Peca na tabela verificacao.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: idverificacaoveiculo
        description: >
          Identificacao do Veiculo na tabela verificacao.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vetiquetaeneida
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vetiquetaeneida.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vfnaprocapproj
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vfnaprocapproj.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: apromec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: capmec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aproart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: capart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aproart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: capart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aproconv
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: capconv
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprocontra
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: capcontra
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vfnaprocapprojmec
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vfnaprocapprojmec.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: apromec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: capmec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vfninccaprenuncia
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vfninccaprenuncia.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: renuncia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vfnorcdetalhado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vfnorcdetalhado.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: promec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: commec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: comart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: comart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vfntotalaprocapproj
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vfntotalaprocapproj.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vforadaportaria
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vforadaportaria.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tipoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: diasvencido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vfsac3485
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vfsac3485.
    columns:
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vfunarte
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vfunarte.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vgerentes
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vgerentes.
    columns:
      - name: org_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: org_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_estrutura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_gerente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nomegerente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: fun_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fun_funcaoorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: usu_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: usu_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: usu_sala
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usu_ramal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usu_nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vguianaovinculada
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vguianaovinculada.
    columns:
      - name: numeroguia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__guiarecolhimento')
                field: numeroguia
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrecolhimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vguiasderecolhimento
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vguiasderecolhimento.
    columns:
      - name: vinculo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: numeroguia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__guiarecolhimento')
                field: numeroguia
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: subscritor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtrecolhimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtvencimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vincentivador
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vincentivador.
    columns:
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vincentivadorano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vincentivadorano.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: mp
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoapoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vincentivadoraudioano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vincentivadoraudioano.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vincentivadoresma
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vincentivadoresma.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vincentivadorpessoaano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vincentivadorpessoaano.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vincentivadorporprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vincentivadorporprojeto.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: date
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vliberado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vliberado.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtliberacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: permissao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vmaioresincentivadoresprojetos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vmaioresincentivadoresprojetos.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: incentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vmostraconvenio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vmostraconvenio.
    columns:
      - name: opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtpublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfinalexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciovigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfinalvigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorconvenio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: objeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vmostraempenho
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vmostraempenho.
    columns:
      - name: nrempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: uggestao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ugr
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ptres
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: naturezadespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vmostraordembancaria
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vmostraordembancaria.
    columns:
      - name: nrordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vnacional
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vnacional.
    columns:
      - name: idendereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoendereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipologradouro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: logradouro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: numero
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: complemento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: divulgar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vnegativainabilitado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vnegativainabilitado.
    columns:
      - name: horgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: hnumerocarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: hdtcarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: hanoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: hsequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pnomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: parea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pprocesso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ianoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: isequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: hlogon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vnegativainabilitadoetiqueta
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vnegativainabilitadoetiqueta.
    columns:
      - name: horgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: hnumerocarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: hdtcarta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: hanoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: hsequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pnomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pcgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: hlogon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vnuncacaptou
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vnuncacaptou.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: temposemcaptar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__voutrasinformacoesaudio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__voutrasinformacoesaudio.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: produtor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: diretor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: roteirista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: metragem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: genero
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: veiculacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: suportegravacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: finalizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: duracaotipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: duracaoqtde
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: duracaocada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: duracaototal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vparecerinicial
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vparecerinicial.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vpautadereuniaofncintercambio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vpautadereuniaofncintercambio.
    columns:
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: evento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: entidadepromotora
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cidadeevento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: pais
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: processo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: resumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: parecertecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: resumocurriculo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlnormal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlpromocional
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlnormaltotal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlpromocionaltotal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtinicio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vportariaaprovado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vportariaaprovado.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vportariacomplementacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vportariacomplementacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vportarianormal
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vportarianormal.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vportariaprorrogacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vportariaprorrogacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vportariareducao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vportariareducao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprazocaptacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprazocaptacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprestacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprestacao.
    columns:
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciorealizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfinalrealizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: valorexecutado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aplicacaofinanceira
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: outrasfontes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: saldorecolhido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprojetoavaliado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprojetoavaliado.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: classificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: peso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprojetodiligencia
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprojetodiligencia.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: diligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprojetosemanalise
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprojetosemanalise.
    columns:
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsaida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: unidadeanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprojetosemexecucao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprojetosemexecucao.
    columns:
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: saldoacaptar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: codigoarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprojetosexecutados
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprojetosexecutados.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprojprazodecaptvencnoexerc
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprojprazodecaptvencnoexerc.
    columns:
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: saldo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vpropapreaproexec
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vpropapreaproexec.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: executado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponenteanouf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponenteanouf.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponenteavaliado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponenteavaliado.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: classificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: peso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponentecaptacaoanouf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponentecaptacaoanouf.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponentecommaisdecincoproj
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponentecommaisdecincoproj.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtde
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponentemecenato
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponentemecenato.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: responsavel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponenteporarea
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponenteporarea.
    columns:
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponenteprojetos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponenteprojetos.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponenteprojetosongs
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponenteprojetosongs.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproponentetipopessoa
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproponentetipopessoa.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: saldo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vpropquereceberbandadenovo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vpropquereceberbandadenovo.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vproprecebeubanda
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vproprecebeubanda.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vpropufareaseg
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vpropufareaseg.
    columns:
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vprorrogacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vprorrogacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: portariaaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtportariaaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtpublicacaoaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: saldoacaptar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vqtdeprojaproanouf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vqtdeprojaproanouf.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrankbanda
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrankbanda.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtprotocolo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempodemanda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pontodemanda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfundacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: antiguidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pontoantiguidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: finalidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pontofinalidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: indicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pontoindicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: emenda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pontoemenda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vregularidade
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vregularidade.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: gerador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrenunciaanoareageral
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrenunciaanoareageral.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: lei
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: area1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area4
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area5
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area6
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area7
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrenunciaanoareaufgeral
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrenunciaanoareaufgeral.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: lei
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: area2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: area3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: area4
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: area5
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: area6
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: area7
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrenunciafiscal
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrenunciafiscal.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: trim1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: trim2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: trim3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: trim4
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrenunciafiscalanoarea
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrenunciafiscalanoarea.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: area1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area4
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area5
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area6
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: area7
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrenunciafiscalgeral
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrenunciafiscalgeral.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: lei
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: trim1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: trim2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: trim3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: trim4
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrenunciageralporanoprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrenunciageralporanoprojeto.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: lei
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: renuncia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrenunciaporano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrenunciaporano.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: mp
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoapoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: apoioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vrenunciaporincentivador
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vrenunciaporincentivador.
    columns:
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: renuncia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vricardoreis
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vricardoreis.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: processo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vsaldoaprovado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vsaldoaprovado.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: apromec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aproart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aproart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aproconv
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprocontra
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: compmec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: compart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: compart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: compconv
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: redumec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: reduart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: reduart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: reduconv
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vsaldoaprovadoporano
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vsaldoaprovadoporano.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: apromec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aproart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aproart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprocusteio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: compmec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: compart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: compart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: redumec
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: reduart1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: reduart3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vsaldoquotascav
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vsaldoquotascav.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: integralizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: cancelado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vsegmento
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vsegmento.
    columns:
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tp_enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vtemposemcaptar
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vtemposemcaptar.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: temposemcaptar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vtermoaditivo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vtermoaditivo.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtfinalvigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vtermodecompromisso
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vtermodecompromisso.
    columns:
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: resumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vtodaaprovacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vtodaaprovacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtprotocolo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: portaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtpublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: mecenato
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: artigo1
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: custeio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: artigo3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vtotalaprovado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vtotalaprovado.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: aprovadoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovadoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: concedidocusteioreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: concedidocapitalreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartidareal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vtotalcaptado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vtotalcaptado.
    columns:
      - name: anorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoufir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vultimafase
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vultimafase.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dttermino
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vusuarios
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vusuarios.
    columns:
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sala
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_ramal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nome_completo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: org_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_estrutura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_gerente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nomegerente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: fun_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fun_funcaoorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vvaloresprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vvaloresprojeto.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: captado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: saldo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vversepodeprorrogar
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vversepodeprorrogar.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: temposemcaptar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vw_captacao_ano_uf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vw_captacao_ano_uf.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vw_dba_captacao_2007_2017
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vw_dba_captacao_2007_2017.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwagentesseusprojetos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwagentesseusprojetos.
    columns:
      - name: ordem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciodeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfinaldeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwalteraranalistaproposta
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwalteraranalistaproposta.
    columns:
      - name: idproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwalterarorgao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwalterarorgao.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwalterarprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwalterarprojeto.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: providenciatomada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwalterarsituacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwalterarsituacao.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: providenciatomada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: logon
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwanalisedeconteudo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwanalisedeconteudo.
    columns:
      - name: idanalisedeconteudo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwanalisedecusto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwanalisedecusto.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaprojeto')
                field: idplanilhaprojeto
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: etapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: unidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vltotal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: data
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: vlcorte
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativasugerida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlsugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwanalisedocumentalportecnico
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwanalisedocumentalportecnico.
    columns:
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtmovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: conformidadeok
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwanalisefinanceiravirtual
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwanalisefinanceiravirtual.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsmecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssituacaoencprestcontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idagentedestino
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: dtinicioencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdiasemanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsituacaoencprestcontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwanalisevisualportecnico
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwanalisevisualportecnico.
    columns:
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtmovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: conformidadeok
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwinformacoes_tcu_etce
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwinformacoes_tcu_etce.
    columns:
      - name: nun_instrumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: ano_instrumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: num_siafi_instrumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: num_siconv_instrumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descr_objeto_instrumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipo_instrumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: se_mandatario_cef
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: programa_projeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: num_nota_empenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: num_ob
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: num_banco_conta_especifica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: num_ag_conta_especifica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: num_conta_especifica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: uf_destino_recurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpj_destinatario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome_destinatario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dt_inicio_vigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dt_fim_vigencia_original
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dt_fim_vigencia_final
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtd_prorrogacoes_vigencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao_execucao_objeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_total_instrumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_total_transferido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor_contrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwinformacoesconsolidadasparaavaliacaofinanceira
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwinformacoesconsolidadasparaavaliacaofinanceira.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviodaprestacaocontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: resultadoavaliacaoobjeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtempregosdiretos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtempregosindiretos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtempregosgerados
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtcomprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtnc_90
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtnc_95
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtnc_99
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwinformacoesdoatoadministrativo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwinformacoesdoatoadministrativo.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipodoatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddocumentoassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dt_criacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idatodegestao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwitemorcamentarioimpugnado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwitemorcamentarioimpugnado.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: etapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stitemavaliado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: documento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpformadepagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdocumentodepagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwitenscomprovados_fonte_produto_etapa_uf_municipio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwitenscomprovados_fonte_produto_etapa_uf_municipio.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaaprovacao')
                field: idplanilhaaprovacao
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idplanilhaetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: descetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaitens
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaitens')
                field: idplanilhaitens
      - name: descitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: total
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: comprovacaovalidada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpcusto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stitemavaliado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwlancamentobancariosemcomprovacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwlancamentobancariosemcomprovacao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdlancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dslancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrlancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtlancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vllancamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwlaudofinalavaliacaoderesultadodoprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwlaudofinalavaliacaoderesultadodoprojeto.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idlaudofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tblaudofinal')
                field: idlaudofinal
      - name: dtlaudofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: simanifestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsmanifestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dslaudofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwliberacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwliberacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: inabilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: certidao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cadin
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwlistarcaptacoes
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwlistarcaptacoes.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: sguf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrlote
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpj_cpf_incentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmincentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: tpapoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipoapoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: sitransferenciarecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dstransferenciarecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dttransferenciarecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: isbemservico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsbemservico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwlistarpropostas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwlistarpropostas.
    columns:
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: stplanoanual
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtadmissibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliacaoproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idmovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbmovimentacao')
                field: idmovimentacao
      - name: sttipodemanda
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwmaioresincentivadoresporanouf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwmaioresincentivadoresporanouf.
    columns:
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpj_cpf_incentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: incentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlincentivado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: ranking
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwmaioresproponentesporanouf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwmaioresproponentesporanouf.
    columns:
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: ranking
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwmediana
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwmediana.
    columns:
      - name: tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idplanilhaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsitemorcamentario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsunidadefederacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtitens
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: simediana
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlmediana
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlminimo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlmedio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlmaximo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwmemoriadecalculo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwmemoriadecalculo.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: valorproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: outrasfontes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: elaboracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorsugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwnatureza
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwnatureza.
    columns:
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: direito
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: esfera
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: poder
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: administracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwnotificacaoaoproponente
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwnotificacaoaoproponente.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idnotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbnotificacao')
                field: idnotificacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpnotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsnotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvionotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtvisualizacaonotificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stvisualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsvisualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vworcamentosolicitado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vworcamentosolicitado.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaproposta')
                field: idplanilhaproposta
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: etapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: unidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vltotal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpagamentodefornecedordoprojetoporitem
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpagamentodefornecedordoprojetoporitem.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: fornecedor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlpago
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpagamentodefornecedordoprojetoporitemdetalhado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpagamentodefornecedordoprojetoporitemdetalhado.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: fornecedor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtemissao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpdocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpformadepagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrdocumentodepagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: vlpago
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpagamentodespesaforaperiodoexecucao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpagamentodespesaforaperiodoexecucao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fornecedor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: obs
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpagamentofinalizadoparecerista
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpagamentofinalizadoparecerista.
    columns:
      - name: tppagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sgunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dthomologacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrdespacho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtgeracaopagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtefetivacaopagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sipagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpagamentofornecedorirregularreceitafederal
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpagamentofornecedorirregularreceitafederal.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsetapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cditem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmempresarial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtabertura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: date
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacaocadastral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: date
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacaocadastral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssituacaocadastral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpagamentoparecerista
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpagamentoparecerista.
    columns:
      - name: idgerarpagamentoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbgerarpagamentoparecerista')
                field: idgerarpagamentoparecerista
      - name: nrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoefetivacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: mesefetivacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdespacho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtgeracaopagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtefetivacaopagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrordembancaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sipagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlpagamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineladequacaorealidadeexecucao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineladequacaorealidadeexecucao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: ufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsplanoexecucaoimediata
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdiligencias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: vlautorizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: vladequado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: vlpercentualcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineladmissibilidade
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineladmissibilidade.
    columns:
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdtipodeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipodeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdtipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdtipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdmovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdconformidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvioproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdiasemanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelaguardandodistribuicaoanalisetecnica
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelaguardandodistribuicaoanalisetecnica.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtenviomincvinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdiasdistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdesecundarios
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelavaliarpropostas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelavaliarpropostas.
    columns:
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtmovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: diasdesdemovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbmovimentacao')
                field: idmovimentacao
      - name: codsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtadmissibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: diascorridos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliacaoproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadoravaliacaotrimestral
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadoravaliacaotrimestral.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtcomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtinicioperiodo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimperiodo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: sicomprovantetrimestral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcomprovantetrimestral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: diligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnicoavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsparecertecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorreadequacaoaguardandoanalise
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorreadequacaoaguardandoanalise.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtaguardandodistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorreadequacaoaguardandoanalise_20180323
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorreadequacaoaguardandoanalise_20180323.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtaguardandodistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorreadequacaoanalisados
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorreadequacaoanalisados.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdiasdistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qtdiasavaliar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qttotaldiasavaliar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sgunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: iddocumentoassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorreadequacaoanalisados_20180323
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorreadequacaoanalisados_20180323.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdiasdistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdiasavaliar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qttotaldiasavaliar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sgunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorreadequacaoemanalise
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorreadequacaoemanalise.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: qtdiasencaminhar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: qtdiasemanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmreceptor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sgunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorreadequacaoemanalise_20180323
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorreadequacaoemanalise_20180323.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: qtdiasencaminhar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: qtdiasemanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmreceptor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sgunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorvinculadasemanalise
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorvinculadasemanalise.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviomincvinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdiasparadistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tempototalanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdesecundarios
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtrespostadiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdiligenciaproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorvinculadasemvalidacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorvinculadasemvalidacao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviomincvinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempototalanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdiligenciaproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: obs
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tecnicovalidador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtvalidacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelcoordenadorvinculadasreanalisar
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelcoordenadorvinculadasreanalisar.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tipoanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtenviomincvinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdiasdistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: justcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: justdevolucaopedido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: justsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldedocumentosaguardandoassinatura
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldedocumentosaguardandoassinatura.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: resumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddocumentoassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: possuiproximaassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidadeassinaturas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidadetotalassinaturas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tp_enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idtipodoatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipodoatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idordemdaassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idperfildoassinante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaodoassinante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaosuperiordoassinante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ordemdaproximaassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldedocumentosdereadequacaoaguardandoassinatura
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldedocumentosdereadequacaoaguardandoassinatura.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipodoatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddocumentoassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipodoatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedepessoasquefaltamassinar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedepessoasqueassinaramdocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdeassinaturasporatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idordemdaassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idperfildoassinante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaodoassinante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaosuperiordoassinante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ordemdaproximaassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldeemissaodelaudofinalderesultado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldeemissaodelaudofinalderesultado.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsresutadodaavaliacaodoobjeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsresutadodaavaliacaofinanceira
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldeemissaodelaudofinalderesultadopassivo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldeemissaodelaudofinalderesultadopassivo.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsresutadodaavaliacaodoobjeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsresutadodaavaliacaofinanceira
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldehomologacaodeprojetos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldehomologacaodeprojetos.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: vlhomologado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldeliberacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldeliberacao.
    columns:
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: percentualcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: inabilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: certidao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cadin
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: providenciatomada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldemensagens
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldemensagens.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: codarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: codsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: idmensagemprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dtmensagem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsmensagem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdtipomensagem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idremetente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmremetente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: dsunidaderemetente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idremetenteunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: iddestinatario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmdestinatario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: iddestinatariounidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsunidadedestinatario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idmensagemorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldetransferenciaderecursos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldetransferenciaderecursos.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cgccpfmecena
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: dtchegadarecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtrecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: captacaoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: numerorecibo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tipoapoio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: incentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtliberacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: inabilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: certidao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cadin
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: percentual
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: idcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldocomponentedacomissao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldocomponentedacomissao.
    columns:
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsenquadramentoproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdenquadramentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsenquadramentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdareaminc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsareaminc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: cdperfilavaliadorproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldocoordenador
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldocoordenador.
    columns:
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipodeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvioproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: qtdiasemanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsenquadramentoproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdenquadramentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsenquadramentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdareaminc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsareaminc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: siestadoanaliseproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsestadoanaliseproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdperfilavaliadorproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsperfilavaliadorproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: stestadorecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldocoordenadorgeral
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldocoordenadorgeral.
    columns:
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipodeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvioproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: qtdiasemanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsenquadramentoproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdenquadramentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsenquadramentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdareaminc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsareaminc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: siestadoanaliseproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsestadoanaliseproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdperfilavaliadorproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsperfilavaliadorproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: stestadorecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineldotecnicodeadmissibilidade
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineldotecnicodeadmissibilidade.
    columns:
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipodeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvioproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: qtdiasemanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsenquadramentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsareaminc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmentominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: cdperfilavaliadorproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelemanalisetecnica
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelemanalisetecnica.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviomincvinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdiasparadistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tempototalanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdesecundarios
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtrespostadiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdiligenciaproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelemvalidacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelemvalidacao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipoanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviomincvinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempototalanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdiligenciaproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: obs
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tecnicovalidador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtvalidacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelenquadramentopropostas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelenquadramentopropostas.
    columns:
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsexecucaoimediata
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsenquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdperfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dscomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelparecerista
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelparecerista.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviomincvinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdiasparadistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tempototalanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tempodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdesecundarios
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtrespostadiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdiligenciaproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: observacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelpareceristaunificado
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelpareceristaunificado.
    columns:
      - name: tipopainel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgaosigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtresposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stenviado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: quantidadeprodutos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tipoanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stdiligenciado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dstiporeadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdiasavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idorgaounidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: iddocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idnrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: statendimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idtiporeadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dssolicitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsorientacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsnomesolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: dsnomeavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: iddocumentoassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idtipodoatoadministrativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: tempofimdiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idavaliadordistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: iddistribuirprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirprojeto')
                field: iddistribuirprojeto
      - name: dssolicitacaorecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbrecurso')
                field: idrecurso
      - name: tprecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sedisponivelassinatura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sediligenciaaberta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sirecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelpresidentevinculadas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelpresidentevinculadas.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tipoanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tecnicovalidador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtvalidacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelprojetossgptc
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelprojetossgptc.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: cdultimasituacaoprescricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtultimasituacaoprescricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vltransferidomovimentacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlperccaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlpercomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldocontacaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldocontamovimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldodascontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlutilizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlpercutilizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelreadequacaoaguardandopublicacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelreadequacaoaguardandopublicacao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sgunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelreadequacaocoordenadorpareceranalisados
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelreadequacaocoordenadorpareceranalisados.
    columns:
      - name: iddistribuirreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirreadequacao')
                field: iddistribuirreadequacao
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdiasdistribuir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdiasavaliar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: qttotaldiasavaliar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelreadequacaocoordenadorpareceremanalise
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelreadequacaocoordenadorpareceremanalise.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: dsavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdiasemanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: iddistribuirreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirreadequacao')
                field: iddistribuirreadequacao
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelreadequacaotecnico
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelreadequacaotecnico.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: qtdiasavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idtecnicoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineltecnicoavaliacaotrimestral
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineltecnicoavaliacaotrimestral.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcomprovante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtinicioperiodo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimperiodo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: sicomprovantetrimestral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcomprovantetrimestral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: diligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnicoavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpaineltransferenciarecursoscaptacaomovimento
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpaineltransferenciarecursoscaptacaomovimento.
    columns:
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlatransferir
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlrecebido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlpercentualcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: stcontaliberada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sthabilitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stcertidao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stcadin
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldodascontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpainelvalidadoscomdevolucao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpainelvalidadoscomdevolucao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtenviomincvinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: tempototalanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tempoparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tempodiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdiligenciaproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valor
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: obs
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: fecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tecnicovalidador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtvalidacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpareceresdereadequacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpareceresdereadequacao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbreadequacao')
                field: idreadequacao
      - name: idtiporeadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenviominc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tpmanifestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsmanifestacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpareceristascredenciados
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpareceristascredenciados.
    columns:
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdorgaosuperior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sgorgaoautorizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrtelefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: eecorreioeletronico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpareceristasorgao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpareceristasorgao.
    columns:
      - name: idparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpautados_cnic_mais_trinta_dias
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpautados_cnic_mais_trinta_dias.
    columns:
      - name: conselheiro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: recurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpecadedivulgacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpecadedivulgacao.
    columns:
      - name: idverificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: idtipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pecadedivulgacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwpedidodeprorrogacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwpedidodeprorrogacao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: anoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sequencial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: codsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: mecanismo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprorrogacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtpedido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: atendimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwplanilhaaprovada
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwplanilhaaprovada.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaaprovacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaaprovacao')
                field: idplanilhaaprovacao
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: etapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: justproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlsugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: justparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: unidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpdespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tppessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcontrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idfonte
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: justcomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwplanilhasugerida
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwplanilhasugerida.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaprojeto')
                field: idplanilhaprojeto
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: etapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativaproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: unidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idfonte
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwplanilhasugeridacompleta
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwplanilhasugeridacompleta.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbplanilhaprojeto')
                field: idplanilhaprojeto
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: etapa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: unidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: quantidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: valorunitario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdedias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: contrapartida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idfonte
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: fonterecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwportariasgeradas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwportariasgeradas.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsreadequacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tipoportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprecomedio_produto_item_unidade_uf_municipio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprecomedio_produto_item_unidade_uf_municipio.
    columns:
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idplanilhaitem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: item
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: unidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idufdespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipiodespesa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: preco_minimo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: preco_medio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: preco_maximo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprocuradordeprojeto
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprocuradordeprojeto.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf_proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprocuracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpfprocurador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: procurador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddocumento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtprocuracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtvinculacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdesvinculacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsjustificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsobservacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idsolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cpfsolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomesolicitante
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: siestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: siprocuracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprodutos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprodutos.
    columns:
      - name: codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprodutosemanalisetecnicanasvinculadas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprodutosemanalisetecnicanasvinculadas.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdorgaosuperior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: providenciatomada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: parecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: datafixa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsencaminhamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sifecharanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtprimeiroenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtultimoenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrcpf_parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: parecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: mesdoultimoenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdiastotalcomparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdiasdeanalisedoparecerita
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdiasemdilgenciapeloparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdediasnavinculada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanet
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanet.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: tipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dataabertura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: datafechamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: objetivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtavaliadores
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtpropostas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tppessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlmaximoproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: datahabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dataselecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dataresultado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrpropostaselecionada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrpontuacaominima
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: vltotalprograma
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetavaliacaofinalizada
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetavaliacaofinalizada.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: nmprograma
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacaominima
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrranking
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetavaliacaoinicialmediasnotasproposta
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetavaliacaoinicialmediasnotasproposta.
    columns:
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dspergunta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetavaliacaoinicialnotasporproposta
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetavaliacaoinicialnotasporproposta.
    columns:
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dspergunta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: siavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrordem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetavaliadores
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetavaliadores.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprogramarouanetavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanetavaliador')
                field: idprogramarouanetavaliador
      - name: qtavaliadores
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpropostas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetdistribuirproposta
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetdistribuirproposta.
    columns:
      - name: idunicolinha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: tpavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetdistribuirpropostarecurso
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetdistribuirpropostarecurso.
    columns:
      - name: idunicolinha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouaneteditais
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouaneteditais.
    columns:
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dssituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: tptipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dsestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: dtabertura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfechamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtresultado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stestadoprograma
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vltotalprograma
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dslocalizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dscontrato
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpjcpfincentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: nmincentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: vlincentivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanethabilitarproposta
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanethabilitarproposta.
    columns:
      - name: idprogramarouanethabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanethabilitacao')
                field: idprogramarouanethabilitacao
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dthabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dshabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sihabilitacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciodeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfinaldeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtabertura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfechamento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtresultado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtavaliadores
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpropostas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tppessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: vlmaximoproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetincentivadorprojetosapoiados
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetincentivadorprojetosapoiados.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sguf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpfcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dscdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtadmissao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsresumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpjcpfincentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmincentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtincentivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dttransferenciarecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: vlincentivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: percentualcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldodiariocontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlutilizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: percentualutilizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: percentualnascontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetnotasporproposta
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetnotasporproposta.
    columns:
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dspergunta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtpontosrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: siavaliacaorecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrordem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetpainelresultadofinal
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetpainelresultadofinal.
    columns:
      - name: idprogramarouanetresultadofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanetresultadofinal')
                field: idprogramarouanetresultadofinal
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: sguf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrrankingpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrrankingfinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsresultadofinal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpselecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlalocadoregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlalocadouf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlalocadoarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdfaixa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetpainelsolicitarrecurso
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetpainelsolicitarrecurso.
    columns:
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioprazorecursal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimprazorecursal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: idusuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetprojetosselecionados
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetprojetosselecionados.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: sguf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrcnpfcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dscdsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtadmissao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idunidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsresumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dsobjetivos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dsacessibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dsdemocratizacaodeacesso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsetapadetrabalho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dsfichatecnica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dssinopse
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsespecificacaotecnica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsestrategiadeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsdescricaoatividade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciocaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtfimcaptacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: percentualcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcomprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldodiariocontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlutilizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: percentualutilizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: percentualnascontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetpropostasavaliadas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetpropostasavaliadas.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: nrpontos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ststatusavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsstatusavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetpropostasnaoavaliadas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetpropostasnaoavaliadas.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: nrpontos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetpropostasrecebidas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetpropostasrecebidas.
    columns:
      - name: idprogramarouanetxproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanetxproposta')
                field: idprogramarouanetxproposta
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtiniciodeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfinaldeexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdcidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetrecursopropostaavaliadas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetrecursopropostaavaliadas.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: nrpontos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: ststatusavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsstatusavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetrecursospropostasnaoavaliadas
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetrecursospropostasnaoavaliadas.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbprogramarouanet')
                field: idprogramarouanet
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nmavaliador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__kitbanda')
                field: codigo
      - name: nrpontosavaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontosrecurso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanetresultadoavaliacaoinicial
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanetresultadoavaliacaoinicial.
    columns:
      - name: idprogramarouanet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmprograma
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nmproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmregiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsuf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrpontuacaominima
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrranking
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprogramarouanettipologia
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprogramarouanettipologia.
    columns:
      - name: idverificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('sac__verificacao')
                field: idverificacao
      - name: idtipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetobeneficiarioprodutocultural
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetobeneficiarioprodutocultural.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: beneficiario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtipobeneficiario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtrecebida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: anopublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetodistribuidovinculada
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetodistribuidovinculada.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricaoanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tipoanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetoorgao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetoorgao.
    columns:
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: produto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: descricaoanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tipoanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: obs
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetos_na_pauta_cnic
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetos_na_pauta_cnic.
    columns:
      - name: analise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: componente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: resumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: codsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: descarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descsegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idnrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: stanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: avaliacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitadoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: sugeridoreal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicaocomponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetosadequadosrealidadeexecucao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetosadequadosrealidadeexecucao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: enquadramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vloutrasfontes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetosapreciadosaguardandohomologacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetosapreciadosaguardandohomologacao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: ufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: prazorecursal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtpublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: nrportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idnrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: nrreuniao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: perccaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetosaprovadossemhomologacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetosaprovadossemhomologacao.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipopessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsexcecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: resumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: objetivos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: justificativa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: democratizacaodeacesso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: acessibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpexcecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vladequado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: percaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetoscominconsistencianacaptacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetoscominconsistencianacaptacao.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cgccpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcpfcnpjproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcpfcnpjincentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmincentivador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: dtcredito
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlvalorcredito
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdpatrocinio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nragenciaproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcontaproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpvalidacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipoinconsistencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipoincentivo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldocontas
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetosconsolidados
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetosconsolidados.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valorproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: outrasfontes
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valorsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valorsugerido
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: elaboracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: valorparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: perc
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: acima
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetosemanalisecomparecerista
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetosemanalisecomparecerista.
    columns:
      - name: tpanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: iddistribuirparecer
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('sac__tbdistribuirparecer')
                field: iddistribuirparecer
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idorgaoorigem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtenvio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: idagenteparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtdistribuicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtcorrecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: dtdevolucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: stestado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sianalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: nrdiasparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdiasdiligencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrdiastotalanalise
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsaldodoempenho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtconcessao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: tpparecerista
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetosgratuitopago
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetosgratuitopago.
    columns:
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: gratuito
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: resumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: objetivos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: acessibilidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: democratizacaodeacesso
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: qtdeproduzida
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlproduto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
      - name: vlautorizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetositinerantes
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetositinerantes.
    columns:
      - name: idprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ufprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtiniciofase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: resumoprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: idfase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetosliberadosparaadequacaorealidadeexecucao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetosliberadosparaadequacaorealidadeexecucao.
    columns:
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: area
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsarea
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: segmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssegmento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: qtdias
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrportaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtpublicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: agencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: contabloqueada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: excecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stdatafixa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: datamarcada
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpprorrogacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: solicitacaoprorrogacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlsolicitado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: perccaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: sac__vwprojetosporfase
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.sac__vwprojetosporfase.
    columns:
      - name: idpronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pronac
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: situacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dttransformacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: dtinicioexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtfimexecucao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: stexcecao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stfluxo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: fase
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idsecretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlaprovado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcaptado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

