version: 2

models:
  - name: tabelas__autorizados
    description: >
      Tabela responsável por registrar permissões, operações e códigos autorizados no
      sistema. Camada bronze tipada a partir de salic_bronze.tabelas__autorizados.
    columns:
      - name: aut_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aut_procedure
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aut_operacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: aut_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: aut_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__avisos
    description: >
      Tabela responsável por armazenar avisos, textos informativos e seus períodos de
      vigência. Camada bronze tipada a partir de salic_bronze.tabelas__avisos.
    columns:
      - name: avi_destino
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: avi_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: avi_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: avi_data_inicio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: avi_data_limite
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: avi_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__bairros
    description: >
      Tabela responsável por cadastrar bairros e sua vinculação com localidades. Camada
      bronze tipada a partir de salic_bronze.tabelas__bairros.
    columns:
      - name: bai_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: bai_localidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: bai_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: bai_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__cadastro
    description: >
      Tabela responsável por registrar dados cadastrais básicos de usuários e contatos.
      Camada bronze tipada a partir de salic_bronze.tabelas__cadastro.
    columns:
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: lotacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: email
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__categorias_pessoa
    description: >
      Tabela responsável por classificar tipos de pessoas e seus órgãos responsáveis. Camada
      bronze tipada a partir de salic_bronze.tabelas__categorias_pessoa.
    columns:
      - name: ctp_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: ctp_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: ctp_orgao_gerente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: ctp_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__cdpe
    description: >
      Tabela responsável por armazenar campos auxiliares utilizados em processos internos.
      Camada bronze tipada a partir de salic_bronze.tabelas__cdpe.
    columns:
      - name: campo2
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campo3
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campo4
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campo5
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: campo6
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__dtproperties
    description: >
      Tabela responsável por propriedades internas de objetos do SQL Server (uso técnico).
      Camada bronze tipada a partir de salic_bronze.tabelas__dtproperties.
    columns:
      - name: id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: objectid
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: property
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: value
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: uvalue
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: lvalue
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: version
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__ect_bairros
    description: >
      Tabela responsável por armazenar dados estruturados de bairros segundo padrões da ECT.
      Camada bronze tipada a partir de salic_bronze.tabelas__ect_bairros.
    columns:
      - name: ectb_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectb_hash
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectb_abreviado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectb_cod_bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__ect_localidades
    description: >
      Tabela responsável por registrar localidades e classificações de acordo com a ECT.
      Camada bronze tipada a partir de salic_bronze.tabelas__ect_localidades.
    columns:
      - name: ectl_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectl_hash
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectl_local
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectl_tipo_local
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectl_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__ect_recusados
    description: >
      Tabela responsável por armazenar endereços recusados segundo regras postais da ECT.
      Camada bronze tipada a partir de salic_bronze.tabelas__ect_recusados.
    columns:
      - name: ectr_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectr_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectr_cep5
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: ectr_cep8
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: ectr_local
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ectr_tipo_local
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: ectr_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: ectr_bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: ectr_bairro_final
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: ectr_tipo_lograd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__emails
    description: >
      Tabela responsável por cadastrar contas de e-mail, domínios, alias e informações de
      uso. Camada bronze tipada a partir de salic_bronze.tabelas__emails.
    columns:
      - name: eml_id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: eml_conta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: eml_email
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: eml_dominio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: eml_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: eml_cpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: eml_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: eml_aliases
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: eml_utilizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: eml_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__eventos
    description: >
      Tabela responsável por registrar logs de eventos, operações e resultados no sistema.
      Camada bronze tipada a partir de salic_bronze.tabelas__eventos.
    columns:
      - name: eve_datahora
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: eve_usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: eve_operacao
        description: >
          -- SP: spRegistraEvento -- eve_operacao = 1005 = VALIDA -- eve_operacao = 1002 =
          DESATIVA
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: eve_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: eve_parametros
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: eve_resultado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: eve_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__funcoes
    description: >
      Tabela responsável por cadastrar funções e seus status de utilização. Camada bronze
      tipada a partir de salic_bronze.tabelas__funcoes.
    columns:
      - name: fun_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: fun_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: fun_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__grupos
    description: >
      Tabela responsável por agrupar usuários/sistemas e definir perfis de acesso. Camada
      bronze tipada a partir de salic_bronze.tabelas__grupos.
    columns:
      - name: gru_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: gru_sistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__sistemas')
                field: sis_codigo
      - name: gru_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__grupos_ares
    description: >
      Tabela responsável por agrupar perfis de acesso específicos do sistema ARES. Camada
      bronze tipada a partir de salic_bronze.tabelas__grupos_ares.
    columns:
      - name: gru_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__grupos')
                field: gru_codigo
      - name: gru_sistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__grupos_minc11
    description: >
      Tabela responsável por agrupar perfis e acessos específicos do sistema MinC11. Camada
      bronze tipada a partir de salic_bronze.tabelas__grupos_minc11.
    columns:
      - name: gru_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__grupos')
                field: gru_codigo
      - name: gru_sistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__gruposxservicos
    description: >
      Tabela responsável por vincular grupos aos serviços disponíveis. Camada bronze tipada a
      partir de salic_bronze.tabelas__gruposxservicos.
    columns:
      - name: gxs_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: gxs_servico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('tabelas__servicos_intranet')
                field: srv_codigo
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__headers
    description: >
      Tabela responsável por armazenar cabeçalhos e metadados de documentos ou registros.
      Camada bronze tipada a partir de salic_bronze.tabelas__headers.
    columns:
      - name: hdr_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: hdr_numero
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: hdr_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: hdr_data
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: hdr_flag
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: hdr_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__indice_palavras
    description: >
      Tabela responsável por indexação de palavras para busca e localização de termos. Camada
      bronze tipada a partir de salic_bronze.tabelas__indice_palavras.
    columns:
      - name: ipl_palavra
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ipl_posicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ipl_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: ipl_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__listainstitucionais
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__listainstitucionais.
    columns:
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__listapessoais
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__listapessoais.
    columns:
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__localidades
    description: >
      Tabela responsável por registrar localidades, tipos e informações de CEP. Camada bronze
      tipada a partir de salic_bronze.tabelas__localidades.
    columns:
      - name: loc_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: loc_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: loc_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: loc_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__logonnt
    description: >
      Tabela responsável por armazenar informações de logons Windows/NT. Camada bronze tipada
      a partir de salic_bronze.tabelas__logonnt.
    columns:
      - name: name
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: data
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__logradouros
    description: >
      Tabela responsável por cadastrar logradouros, limites, CEPs e bairros relacionados.
      Camada bronze tipada a partir de salic_bronze.tabelas__logradouros.
    columns:
      - name: log_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: log_localidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_limites
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_bairro_final
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: log_cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__mei_grupos_intranet
    description: >
      Tabela responsável por agrupar permissões de grupos na intranet. Camada bronze tipada a
      partir de salic_bronze.tabelas__mei_grupos_intranet.
    columns:
      - name: gri_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: gri_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__mei_gruposxservicos_intranet
    description: >
      Tabela responsável por vincular grupos da intranet aos serviços disponíveis. Camada
      bronze tipada a partir de salic_bronze.tabelas__mei_gruposxservicos_intranet.
    columns:
      - name: gxs_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: gxs_servico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__mei_servicos_intranet
    description: >
      Tabela responsável por cadastrar serviços da intranet e suas propriedades
      visuais/funcionais. Camada bronze tipada a partir de
      salic_bronze.tabelas__mei_servicos_intranet.
    columns:
      - name: srv_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: srv_titulo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: srv_link
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: srv_janela
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: srv_ordem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: srv_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: srv_ativo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: srv_corfonte
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: srv_corfundo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: srv_temsub
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__mei_usuariosxservicos_intranet
    description: >
      Tabela responsável por vincular usuários da intranet aos serviços permitidos. Camada
      bronze tipada a partir de salic_bronze.tabelas__mei_usuariosxservicos_intranet.
    columns:
      - name: uxs_usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uxs_servicos
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__menus
    description: >
      Tabela responsável por cadastrar menus, opções e estruturas de navegação do sistema.
      Camada bronze tipada a partir de salic_bronze.tabelas__menus.
    columns:
      - name: men_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: men_sistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__sistemas')
                field: sis_codigo
      - name: men_modulo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_menu
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_exibicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: men_aplicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__menus_ares
    description: >
      Tabela responsável por configurar menus e navegação do sistema ARES. Camada bronze
      tipada a partir de salic_bronze.tabelas__menus_ares.
    columns:
      - name: men_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__menus')
                field: men_codigo
      - name: men_sistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_modulo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_menu
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_exibicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: men_aplicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__menus_minc11
    description: >
      Tabela responsável por configurar menus e navegação do sistema MinC11. Camada bronze
      tipada a partir de salic_bronze.tabelas__menus_minc11.
    columns:
      - name: men_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__menus')
                field: men_codigo
      - name: men_sistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_modulo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_menu
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_opcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_exibicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: men_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: men_aplicacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__menusxgrupos
    description: >
      Tabela responsável por vincular menus aos grupos de acesso. Camada bronze tipada a
      partir de salic_bronze.tabelas__menusxgrupos.
    columns:
      - name: mxg_menu
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__menus')
                field: men_codigo
      - name: mxg_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__grupos')
                field: gru_codigo
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__menusxgrupos_ares
    description: >
      Tabela responsável por vincular menus aos grupos do sistema ARES. Camada bronze tipada
      a partir de salic_bronze.tabelas__menusxgrupos_ares.
    columns:
      - name: mxg_menu
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mxg_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__menusxgrupos_minc11
    description: >
      Tabela responsável por vincular menus aos grupos do MinC11. Camada bronze tipada a
      partir de salic_bronze.tabelas__menusxgrupos_minc11.
    columns:
      - name: mxg_menu
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: mxg_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__meta_dados
    description: >
      Tabela responsável por metadados de tabelas, tamanho, tipo, critérios e ocorrências.
      Camada bronze tipada a partir de salic_bronze.tabelas__meta_dados.
    columns:
      - name: met_tabela
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: met_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: met_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: met_ocorrencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: met_tamanho
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: met_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: met_criterio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: met_indice_palavras
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: met_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__orgaos
    description: >
      Tabela responsável por cadastrar órgãos, siglas, gerente, vínculos e informações
      administrativas. Camada bronze tipada a partir de salic_bronze.tabelas__orgaos.
    columns:
      - name: org_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: org_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__pessoas')
                field: pes_codigo
      - name: org_gerente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: org_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_cei
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: org_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: org_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__orgaos_ares
    description: >
      Tabela responsável por armazenar órgãos e dados institucionais específicos do sistema
      ARES. Camada bronze tipada a partir de salic_bronze.tabelas__orgaos_ares.
    columns:
      - name: org_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__orgaos')
                field: org_codigo
      - name: org_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_gerente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: org_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_cei
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: org_uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__orgaos_subordinados
    description: >
      Tabela responsável por registrar a hierarquia entre órgãos e seus níveis de
      subordinação. Camada bronze tipada a partir de
      salic_bronze.tabelas__orgaos_subordinados.
    columns:
      - name: sub_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sub_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: sub_nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__palavras
    description: >
      Tabela responsável por armazenar palavras indexadas e seus status para busca. Camada
      bronze tipada a partir de salic_bronze.tabelas__palavras.
    columns:
      - name: pal_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pal_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pal_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__palavras2
    description: >
      Tabela responsável por registrar conjunto paralelo de palavras para indexação e
      consulta. Camada bronze tipada a partir de salic_bronze.tabelas__palavras2.
    columns:
      - name: pal_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__palavras')
                field: pal_codigo
      - name: pal_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pal_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoa_dados
    description: >
      Tabela responsável por armazenar metadados e valores vinculados a pessoas. Camada
      bronze tipada a partir de salic_bronze.tabelas__pessoa_dados.
    columns:
      - name: pdd_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__pessoas')
                field: pes_codigo
      - name: pdd_meta_dado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pdd_sequencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pdd_dado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoa_dados_bkp
    description: >
      Tabela responsável por backup de dados e metadados associados às pessoas. Camada bronze
      tipada a partir de salic_bronze.tabelas__pessoa_dados_bkp.
    columns:
      - name: pdd_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pdd_meta_dado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pdd_sequencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pdd_dado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoa_enderecos
    description: >
      Tabela responsável por cadastrar endereços das pessoas, tipos, CEP e localidades.
      Camada bronze tipada a partir de salic_bronze.tabelas__pessoa_enderecos.
    columns:
      - name: pen_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__pessoas')
                field: pes_codigo
      - name: pen_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pen_endereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: pen_bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: pen_cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: pen_localidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__localidades')
                field: loc_estruturado
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoa_identificacoes
    description: >
      Tabela responsável por registrar identificações e documentos das pessoas. Camada bronze
      tipada a partir de salic_bronze.tabelas__pessoa_identificacoes.
    columns:
      - name: pid_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__pessoas')
                field: pes_codigo
      - name: pid_meta_dado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pid_sequencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pid_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoa_identificacoes_alteradas
    description: >
      Tabela responsável por registrar alterações históricas das identificações das pessoas.
      Camada bronze tipada a partir de salic_bronze.tabelas__pessoa_identificacoes_alteradas.
    columns:
      - name: pia_datahora
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: pia_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pia_anterior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: pia_atual
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoa_identificacoes_bkp
    description: >
      Tabela responsável por backup de identificações associadas às pessoas. Camada bronze
      tipada a partir de salic_bronze.tabelas__pessoa_identificacoes_bkp.
    columns:
      - name: pid_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pid_meta_dado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pid_sequencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pid_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoas
    description: >
      Tabela responsável por cadastrar pessoas, categorias, utilidade pública, orgãos
      vinculados e status. Camada bronze tipada a partir de salic_bronze.tabelas__pessoas.
    columns:
      - name: pes_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: pes_categoria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__categorias_pessoa')
                field: ctp_codigo
      - name: pes_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__tipos_pessoa')
                field: tpe_codigo
      - name: pes_esfera
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_administracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_utilidade_publica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_validade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_orgao_cadastrador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_usuario_cadastrador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_data_cadastramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_orgao_atualizador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_usuario_atualizador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_data_atualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: pes_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoas_bkp
    description: >
      Tabela responsável por armazenar backup de dados cadastrais de pessoas. Camada bronze
      tipada a partir de salic_bronze.tabelas__pessoas_bkp.
    columns:
      - name: pes_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('tabelas__pessoas')
                field: pes_codigo
      - name: pes_categoria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_esfera
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_administracao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_utilidade_publica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_validade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_orgao_cadastrador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_usuario_cadastrador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_data_cadastramento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: pes_orgao_atualizador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_usuario_atualizador
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pes_data_atualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
      - name: pes_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__pessoasxfuncoes
    description: >
      Tabela responsável por vincular pessoas às funções e entidades associadas. Camada
      bronze tipada a partir de salic_bronze.tabelas__pessoasxfuncoes.
    columns:
      - name: pxf_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('tabelas__pessoas')
                field: pes_codigo
      - name: pxf_funcao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('tabelas__funcoes')
                field: fun_codigo
      - name: pxf_entidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: pxf_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__servicos_intranet
    description: >
      Tabela responsável por cadastrar serviços disponíveis na intranet e suas
      características. Camada bronze tipada a partir de
      salic_bronze.tabelas__servicos_intranet.
    columns:
      - name: srv_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: srv_titulo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: srv_link
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: srv_janela
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: srv_ordem
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: srv_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__sistemas
    description: >
      Tabela responsável por registrar sistemas, siglas, URLs, segurança e status de
      utilização. Camada bronze tipada a partir de salic_bronze.tabelas__sistemas.
    columns:
      - name: sis_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sis_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sis_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sis_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sis_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sis_url
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sis_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__sistemas_ares
    description: >
      Tabela responsável por registrar sistemas específicos da plataforma ARES. Camada bronze
      tipada a partir de salic_bronze.tabelas__sistemas_ares.
    columns:
      - name: sis_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('tabelas__sistemas')
                field: sis_codigo
      - name: sis_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sis_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sis_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sis_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sis_url
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: sis_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__sysdiagrams
    description: >
      Tabela responsável por armazenar diagramas e estruturas criadas no SQL Server. Camada
      bronze tipada a partir de salic_bronze.tabelas__sysdiagrams.
    columns:
      - name: name
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: principal_id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: diagram_id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: version
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: definition
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__tabelas
    description: >
      Tabela responsável por registrar informações sobre tabelas, tipos, status e descrição.
      Camada bronze tipada a partir de salic_bronze.tabelas__tabelas.
    columns:
      - name: tab_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tab_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tab_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tab_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__temp_tbl
    description: >
      Tabela responsável por armazenar textos temporários para uso interno da aplicação.
      Camada bronze tipada a partir de salic_bronze.tabelas__temp_tbl.
    columns:
      - name: tmp_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__tipos_logradouro
    description: >
      Tabela responsável por cadastrar tipos de logradouros e abreviações. Camada bronze
      tipada a partir de salic_bronze.tabelas__tipos_logradouro.
    columns:
      - name: tlg_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tlg_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tlg_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__tipos_pessoa
    description: >
      Tabela responsável por definir tipos de pessoa e características jurídicas. Camada
      bronze tipada a partir de salic_bronze.tabelas__tipos_pessoa.
    columns:
      - name: tpe_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tpe_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tpe_pf_pj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tpe_direito
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tpe_fim
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: tpe_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__usuarios
    description: >
      Tabela responsável por registrar usuários, credenciais, permissões, dados de segurança
      e vínculos. Camada bronze tipada a partir de salic_bronze.tabelas__usuarios.
    columns:
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__pessoas')
                field: pes_codigo
      - name: usu_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sala
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_ramal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_exibicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sql_login
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sql_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_duracao_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_data_validade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_limite_utilizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_validacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_data_atualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_conta_nt
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: usu_dica_intranet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_localizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_andar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usu_telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__usuarios_ares
    description: >
      Tabela responsável por armazenar usuários e permissões específicas do sistema ARES.
      Camada bronze tipada a partir de salic_bronze.tabelas__usuarios_ares.
    columns:
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__usuarios')
                field: usu_codigo
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sala
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_ramal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_exibicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sql_login
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sql_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_duracao_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_data_validade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_limite_utilizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_validacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_data_atualizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_conta_nt
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: usu_dica_intranet
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_controle
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_localizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_andar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__usuariosxgrupos_intranet
    description: >
      Tabela responsável por vincular usuários a grupos de acesso da intranet. Camada bronze
      tipada a partir de salic_bronze.tabelas__usuariosxgrupos_intranet.
    columns:
      - name: uxg_usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__usuarios')
                field: usu_codigo
      - name: uxg_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__usuariosxorgaosxgrupos
    description: >
      Tabela responsável por relacionar usuários, órgãos e grupos de acesso. Camada bronze
      tipada a partir de salic_bronze.tabelas__usuariosxorgaosxgrupos.
    columns:
      - name: uog_usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__usuarios')
                field: usu_codigo
      - name: uog_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__orgaos')
                field: org_codigo
      - name: uog_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__grupos')
                field: gru_codigo
      - name: uog_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__usuariosxorgaosxgrupos_ares
    description: >
      Tabela responsável por registrar vínculos entre usuários, órgãos e grupos no ambiente
      ARES. Camada bronze tipada a partir de
      salic_bronze.tabelas__usuariosxorgaosxgrupos_ares.
    columns:
      - name: uog_usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uog_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uog_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__usuexternosistemas
    description: >
      Tabela responsável por armazenar usuários externos, senhas, nomes e permissões de
      acesso aos sistemas. Camada bronze tipada a partir de
      salic_bronze.tabelas__usuexternosistemas.
    columns:
      - name: usuidentificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usunome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: ususenha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
      - name: usuobs
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usustatus
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_distritos_por_palavra
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_distritos_por_palavra.
    columns:
      - name: loc_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__localidades')
                field: loc_codigo
      - name: pal_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_estados_por_palavra
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_estados_por_palavra.
    columns:
      - name: loc_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('tabelas__localidades')
                field: loc_codigo
      - name: pal_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_hierarquia_orgaos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_hierarquia_orgaos.
    columns:
      - name: org_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__orgaos')
                field: org_codigo
      - name: org_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sup_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sup_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sup_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_localidade_completa
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_localidade_completa.
    columns:
      - name: loc_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__localidades')
                field: loc_codigo
      - name: loc_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: loc_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: loc_pais
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_estado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_distrito
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_logradouro_completo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_logradouro_completo.
    columns:
      - name: log_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__logradouros')
                field: log_codigo
      - name: log_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_localidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_limites
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: log_bairro_final
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: log_cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_pais
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_estado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_distrito
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: tlg_descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: bai_bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: bai_bairro_final
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_municipios_por_palavra
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_municipios_por_palavra.
    columns:
      - name: loc_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: loc_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('tabelas__localidades')
                field: loc_codigo
      - name: pal_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_paises_por_palavra
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_paises_por_palavra.
    columns:
      - name: loc_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_estruturado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: loc_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__localidades')
                field: loc_codigo
      - name: pal_texto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_pessoas_nome_completo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_pessoas_nome_completo.
    columns:
      - name: pes_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__pessoas')
                field: pes_codigo
      - name: pes_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: pes_validade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: pes_nome_completo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: pes_nome_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_usuario_completo
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_usuario_completo.
    columns:
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__usuarios')
                field: usu_codigo
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sala
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_ramal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_exibicao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sql_login
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sql_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_duracao_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_data_validade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_limite_utilizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_senha
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_validacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_flag_validacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_flag_seguranca
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nome_completo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: org_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nome_completo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_estrutura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_usuario_orgaos_grupos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_usuario_orgaos_grupos.
    columns:
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__usuarios')
                field: usu_codigo
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nome_completo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__orgaos')
                field: org_codigo
      - name: org_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_gerente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: org_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_cei
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: org_tipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nome_completo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: uog_usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uog_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uog_grupo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_sistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__v_usuarios
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__v_usuarios.
    columns:
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__usuarios')
                field: usu_codigo
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_pessoa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_sala
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_ramal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nivel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nome_completo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__vwrelservidorhpimpressao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__vwrelservidorhpimpressao.
    columns:
      - name: identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nomefuncionario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: siglalotacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricaolotacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__vwusuarios
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__vwusuarios.
    columns:
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__usuarios')
                field: usu_codigo
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_lotacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_localizacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_andar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usu_telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: tabelas__vwusuariosorgaosgrupos
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.tabelas__vwusuariosorgaosgrupos.
    columns:
      - name: usu_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__usuarios')
                field: usu_codigo
      - name: usu_identificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_orgaolotacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usu_telefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: org_superior
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uog_orgao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_siglaautorizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: org_nomeautorizado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: sis_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__sistemas')
                field: sis_codigo
      - name: sis_sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sis_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: gru_codigo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - relationships:
              arguments:
                to: ref('tabelas__grupos')
                field: gru_codigo
      - name: gru_nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uog_status
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

