version: 2

models:
  - name: agentes__vpais
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vpais.
    columns:
      - name: idpais
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: continente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vperfil
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vperfil.
    columns:
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: caracteristica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vprefeiturasregiaouf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vprefeiturasregiaouf.
    columns:
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: perfil
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: prefeitura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vsistema
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vsistema.
    columns:
      - name: idsistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vtelefones
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vtelefones.
    columns:
      - name: idtelefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: tipotelefone
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: ddd
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: numero
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: divulgar
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vuf
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vuf.
    columns:
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: sigla
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: regiao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vufmunicipio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vufmunicipio.
    columns:
      - name: iduf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: idmunicipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: municipio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vverificacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vverificacao.
    columns:
      - name: idverificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idtipo
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: descricao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sistema
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vvinculacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vvinculacao.
    columns:
      - name: idvinculacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idvinculado
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: idvinculoprincipal
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vvisao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vvisao.
    columns:
      - name: idvisao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: visao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vwcadastrarparecerista
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vwcadastrarparecerista.
    columns:
      - name: idagente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idnome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: tiponome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nome
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: idendereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: tipoendereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: tipologradouro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: logradouro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: numero
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: bairro
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: complemento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: cidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: uf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: cep
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: divulgarendereco
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: correspondencia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: usuario
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vwempresajuridica
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vwempresajuridica.
    columns:
      - name: nrcnpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: tpestabelecimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsestabelecimento
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmempresarial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmfantasia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdsituacaocadastral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dssituacaocadastral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtsituacaocadastral
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: date
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdnaturezajuridica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsnaturezajuridica
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: dtabertura
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: date
        tests:
          - not_null   # inferido do perfil do banco
      - name: eecorreioeletronico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: vlcapitalsocial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: numeric
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdporteempresa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsporteempresa
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sioptantesimples
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: sioptantemei
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vwempresajuridicasocios
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vwempresajuridicasocios.
    columns:
      - name: idempresajuridicasocios
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
          - unique   # inferido do perfil do banco
      - name: nrcnpj
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmempresarial
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tpsocio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstiposocio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: nrcnpjcpfsocio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmsocio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtinclusaosociedade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: date
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdqualificacao
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsqualificacaosocio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vwportifolio
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vwportifolio.
    columns:
      - name: proponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: dtportifolio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: timestamp
        tests:
          - not_null   # inferido do perfil do banco
      - name: siportifolio
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: responsavel
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

  - name: agentes__vwpropostasemavaliacao
    description: >
      [NÃO VERIFICADO] Objeto do SALIC ausente do dicionário de dados original (export
      SchemaSpy). Semântica e grão ainda não confirmados. Camada bronze tipada a partir de
      salic_bronze.agentes__vwpropostasemavaliacao.
    columns:
      - name: idpreprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nomeprojeto
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipicidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: tptipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dstipologia
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: stproposta
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: dsplanoexecucaoimediata
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: secretaria
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        tests:
          - not_null   # inferido do perfil do banco
      - name: ano
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: conformidade
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: idtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmtecnico
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: cdproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
        tests:
          - not_null   # inferido do perfil do banco
      - name: cnpjcpf
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.Sensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: nmproponente
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
        tests:
          - not_null   # inferido do perfil do banco
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

