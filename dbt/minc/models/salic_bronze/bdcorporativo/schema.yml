version: 2

models:
  - name: bdcorporativo__sysdiagrams
    description: >
      Tabela responsável por armazenar diagramas do SQL Server criados no ambiente. Camada
      bronze tipada a partir de salic_bronze.bdcorporativo__sysdiagrams.
    columns:
      - name: name
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
        meta:
          openmetadata:
            tags:
              - PII.NonSensitive
      - name: principal_id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: diagram_id
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: version
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: integer
      - name: definition
        description: >
          [NÃO VERIFICADO] Coluna ausente do dicionário de dados original.
        data_type: text
      - name: _fatia
        description: >
          Coluna técnica da ingestão: identifica a fatia de extração que trouxe a linha. Não
          é campo de negócio.
        data_type: integer

