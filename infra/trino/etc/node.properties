# Identidade do nó. Em cluster de um nó só, o node.id é gerado pelo próprio
# Trino se omitido — deixar em branco evita que dois containers subam com o
# mesmo id ao clonar o compose.
node.environment=minc
node.data-dir=/data/trino
